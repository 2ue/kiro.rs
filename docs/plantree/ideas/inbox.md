# Ideas Inbox

- Future: expose capability-level body processing toggles in admin UI only after the internal default profiles are stable.
- Future: add a route planner abstraction that chooses target before parsed-body stages when enough raw facts are available.
- Future: add a memory budget guard around payload guard and conversion diagnostics.
