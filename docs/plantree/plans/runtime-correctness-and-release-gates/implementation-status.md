# Implementation Status

Date: 2026-07-23

## Current Phase

`released / v0.0.114` from work commit `b528ead` plus release bump commit `beb9b34`. Current detailed state lives in [feature/implementation-status.md](../../../../feature/implementation-status.md); this file is the short plan-tree handoff. 2026-07-22 current candidate evidence covers real Claude CLI long session, bare invoke, thinking wire, body/reasoning, runtime quarantine, scheduler Redis chaos and business/observability Redis fault-domain. 2026-07-23 final release gate evidence exists for repository-external `kiro-rs` SHA `925525419cd48b460217df2568891a40287da0c44d2bf921a38b103c047775ee` and `kiro_loadtest` SHA `90babda7388aa93854cbbdb81c132cc436c07f46b0ea22973531b0a7ffb3aff1`; Rust C0/release build, feature docs, Node contracts, `git diff --check` and build inventory all passed. The existing `127.0.0.1:9022` service was not stopped, restarted, migrated or load-tested. Tag `v0.0.114` was pushed and peels to `beb9b3420b20776db489461d65392b5b1d6e5d92`.

## Active TODO

1. Push the post-release documentation commit that records `v0.0.114`; do not move the published tag.
2. Keep Docker dynamic validation recorded as a user waiver for this phase; non-Docker validation programs and contracts remain in place and Docker is not recorded as pass.
3. Continue post-release observation via read-only production evidence if new recurrence samples appear.

## Done This Phase

- Created the root [`feature/`](../../../../feature/README.md) delivery tree with per-issue reproduction/design/acceptance documents, an executable matrix and evidence index.
- Landed focused protocol contamination fail-closed, shared attempt budget, provider 400 classification, scheduler degraded fallback, remote multimodal bounds, upgrade, AWS lifecycle and usage-cleanup work; each remains scoped by its issue status. A later 2026-07-21 pure Node protocol-contamination source contract passed `10/10` and locks the non-Hash/general transcript class, raw-body identity guard, signed/redacted atomic policy and fail-closed terminal paths without starting Docker, Cargo or `kiro.rs`; a follow-up production marker inventory source contract passed `4/4` and locks marker locations, inert placeholders and no rejected tool-result textification.
- Usage cleanup's isolated PostgreSQL/Redis group passed 36/36 in each of three outer runs (108/108, 0 ignored), including rollup consistency, watermark, recovery, in-flight commit and high-cardinality cases.
- A shared writer / exclusive cleanup transaction advisory-lock guard is present; its in-flight commit race test passed three internal rounds.
- Post-contract focused PostgreSQL results: round-trip 1/1, 1000 external billing cleanup 1/1, replay/new-ID internal 3/3 and in-flight guard internal 3/3.
- Split ordinary PgSQL runtime-mutation backlog from dispatch quarantine. Automatic health/stat patches remain dispatchable, while Disable and patches that change disabled/reason/generation remain fail closed. The field matrix, 40-account backlog and 40x15/60-RPM/global-500 tests each passed five internal rounds.
- Removed finite local/external queue renewal amplification: one lease now covers the request-scoped frozen wait deadline plus 60 seconds, while unlimited waits remain renewable. The 500-guard, runtime-config deadline, local/external policy and 40x15/global-500 contracts each passed five internal rounds; 2026-07-19 isolated PostgreSQL/Redis pool-pressure, 22-second deadline, degraded-waiter and cancel programs passed three outer rounds.
- Merged Redis usage snapshot, aggregate and seen into one guarded Lua RTT; capped exact cache-read buckets and replaced the 64-future waiter fanout with a serial shared deadline. Two focused contracts passed five internal rounds each, and the current-project isolated Redis dynamic runner passed 3 outer rounds × 2 tests after a test-only sentinel reset helper preserved the product fail-closed `clear_usage_summary()` contract. The 2026-07-20 normal writer/scheduler burst then passed 3 outer × 3 internal measurements: writer p99 max `31.482 ms`, scheduler loaded/recovery p99 max `55.785/69.204 ms`, RSS delta max about `8.9 MiB`, and FD stayed `15`.
- Removed runtime-hint ownership from the local-capacity fallback decision. Static external eligibility plus enabled capacity fallback now uses fail-fast local acquire, preventing the preflight-ready/acquire-full race from entering the default 120-second local queue. Policy plus manager global-full, CapacityFull and alternate-reselect contracts passed five internal rounds with queue depth zero; storage, burst and frozen-candidate gates remain open.
- Closed the process-local OAuth auxiliary-budget/cancellation red: the first focused run exposed a permit still held after caller cancellation (21/22), the structured refresh-future repair then passed 23/23. The 128-account shared and 20-account independent 12-class c1/c8/c32 matrices, 32-waiter singleflight and default 60/8 admission passed; Redis/PostgreSQL cluster, real provider/CLI attribution and frozen load remain open.
- Closed the development-level thinking wire and frozen fake-upstream Kiro wire: current runner path/contract `11/11` and lifecycle `42/42`, Rust thinking `104/104`, effort `17/17`, provider byte capture 80 requests, converter-to-provider `max` capture 20 requests, real Claude CLI ingress `30/30`, and Kiro wire `60/60` passed. The final wire retains `output_config.effort=max`, does not invent an unadvertised `thinking` field, and records Claude CLI `HEAD /cc` as a protocol fact. Generic Bash/Read long sessions passed separately; real-upstream and active/passive thinking long-session gates remain open.
- Closed the frozen fake-upstream real CLI long-session sub-gate on 2026-07-20: Claude Code CLI 2.1.197 used one `--session-id` followed only by `--continue` in five isolated sessions. The 5x20 and 5x100 matrices passed 110/510 CLI turns, 210/1010 inference hits and 100/500 Bash/Read tool pairs with zero hash/scaffold leak markers, stable non-zero usage, growing history and complete cleanup. Report SHA-256 values are `f8a5faa3...` and `cd79f11b...`; native Kiro upstream and other capabilities remain open.
- Closed the normal and fault-injected single-instance scheduler/Redis sub-gates on 2026-07-20/21: `usage-scheduler-quarantine-r1` completed nine normal joint-pressure measurements, `scheduler-redis-chaos-r2` completed 7 tests × 3 outer (`21/21`) across 50/500ms latency, breaker/backoff, disconnect/reconnect, 300 lease releases, cancel and commit-unknown with zero `AllDisabled`, and `scheduler-redis-joint-chaos-20260721-r6` completed 8 exact × 3 outer (`24/24`) after fixing deterministic WRONGTYPE commit-unknown classification. Usage-writer/scheduler single-instance simultaneous fault is now closed; external takeover and two-instance fault/fallback coordination remain open.
- Closed the shared cross-instance scheduler RPM hole on 2026-07-21: `rpm-reservation-focused-20260721-r4` passed the Redis selection reservation round-trip and the two-manager third-selection block, `cargo check --all-targets` passed, and the frozen candidate `/tmp/kiro-e03-candidate.T2iG7N/kiro-rs` passed the real two-process E03 gate for three outer rounds. This closes the false-open RPM hole but does not by itself close two-instance fault/fallback, external takeover, real upstream or release inventory.
- Closed the business/observability Redis fault-domain product/source gate on 2026-07-21: pure Node contract default 28/28 early-reject pass with 9 live-signal skips, live signal 37/37 pass, baseline fault-domain runner 3 outer pass, product runner `redis-fault-domain-product-r2` 3 outer × 1 exact × 3 internal rounds with 3/3 exact invocations passed, scheduler Redis failure classification fix still preserved the contract in `redis-fault-domain-product-20260721-r4`, and Rust 1.92.0 scoped C0 subset `redis-fault-domain-c0-r2` passed fmt, diff, four observability config tests and `cargo check --all-targets`. Later source-contract expansion, RedisStore production role guard, and business/observability path-isolation contracts passed 37/46 with 9 live-signal skips and locked production injection/entrypoints so usage/Admin/cleanup cannot receive or directly use business Redis, scheduler/external/runtime-event/health use business Redis only, observability Redis startup failure does not fall back to business Redis, and UsageRecorder request handling only enqueues observability writes with pressure drops instead of blocking; scoped `cargo +1.92.0 check --bin kiro-rs` under `redis-observability-role-guard-20260721` also passed. Product scope `1714572 KiB`, post-fix product scope `1708364 KiB`, C0 scope `2051072 KiB`, and guard-check scope `446876 KiB` all reported `removed=true` and `reservation_released=true`; the Rust 1.86 default-toolchain C0 red is recorded as an invalid gate, not a source failure. Two-instance real processes, external takeover, production cardinality and final release gates remain open.
- Added the external-takeover focused/runner gate on 2026-07-21: source review confirmed the `SchedulerRedisDegraded` fallback toggle, fresh-state guard and external eligibility chain; `external-takeover-focused-20260721-r2` passed four exact handler/fallback tests and cleaned `1708372 KiB`; `external-takeover-scheduler-degraded-nondocker` contract 8/8 passed with no Docker/Cargo, no protected `9022` probe, loopback-only PG/Redis and caller-owned prefix checks. Dynamic service execution remains open until an owned empty PostgreSQL database URL is available.
- Added the E01/E02 scheduler fairness runner-contract gate on 2026-07-21: `scheduler-fairness-sticky-race.mjs` now requires caller-owned PG/Redis, no longer invokes Docker, no longer `FLUSHDB`s Redis and no longer creates PostgreSQL databases; each case uses a dedicated Redis `keyPrefix`. The new contract test passed 7/7, runtime path contract 9/9, external takeover contract 8/8, and `git diff --check` passed. Dynamic distribution/sticky/lease-race execution remains open.
- Added the E05 non-Docker runner contract on 2026-07-21: `strict-local-first-routing.mjs` now requires a frozen external binary, owned artifact root, `modes × rounds` pre-created `kiro_e05_*` PostgreSQL databases, a loopback Redis DB1..15 and a caller-owned Redis prefix. It no longer starts Docker, creates databases, `FLUSHDB`s Redis, calls Cargo or probes protected `9022`; Redis fault injection uses `redis-chaos-proxy.mjs`; child processes use minimal env rather than inheriting caller PG/Redis URL env. The runner contract passed 6/6, and the combined runner-contract batch passed 30/30 across runtime path, external takeover, E01/E02 and E05; `git diff --check` passed. A new minimal-env assertion first failed on `startService` inheriting `process.env`, then passed after repair. This does not close E05 dynamic.
- Added F06 and request-admission non-Docker runner contracts on 2026-07-21. F06 contract passed 6/6; request-admission contract passed 5/5; the combined request-admission/F06/runtime-path/external-takeover/E01/E02/E05 runner batch passed 41/41. Both runners now avoid Docker/Cargo/database creation/Redis flush/protected `9022` probes and require caller-owned PG/Redis plus external frozen binary/artifact root. Dynamic F06 and request-admission remain pending.
- Added the frozen load/chaos non-Docker runner contract on 2026-07-21. `frozen-load-chaos-runner.mjs` now requires external frozen `kiro-rs` and `kiro_loadtest` binaries, caller-owned artifact root, tier-sized pre-created `kiro_load_chaos_*` PostgreSQL databases, loopback Redis DB1..15 and a caller-owned Redis prefix. It no longer starts Docker, creates/drops databases, `FLUSHDB`s Redis, calls Cargo, probes protected `9022`, or inherits full `process.env` into fake/proxy/loadtest children. Contract 6/6 passed; runtime path batch 15/15 passed. Final L3/L4/L5 dynamic rebind remains pending.
- Removed runner-level Redis `FLUSHDB` cleanup from the token-refresh cluster and multi-instance Redis coordination runners on 2026-07-21. They still require caller-confirmed empty DB1..15 before Cargo, but post-run residue is now reported as `residualKeyCount` and fails the gate instead of clearing the whole database. Pure Node contracts passed `19 pass / 1 skip / 0 fail`; the skip is explicit live nonempty Redis opt-in. Dynamic reruns remain pending.
- Added shared runner child-environment isolation on 2026-07-21. `feature/tests/validation-child-env.mjs` now gives real validation runner children a whitelist environment instead of inheriting caller `process.env`; Claude/runtime/scheduler/fault-domain runner children were routed through it. `runtime-validation-paths.test.mjs` now passes `11/11`, including non-inheritance checks for PostgreSQL/Redis URLs, Anthropic/OpenAI/request keys and unpassed `KIRO_*` variables, and all non-test validation runners are free of `...process.env`. This closes only validation contamination risk; no product dynamic gate is counted as passed.
- External dispatch storage dynamic passed on current-project isolated PostgreSQL/Redis: `external-dispatch-storage-real-20260718-r3` ran 3 outer rounds × 17 filters, 51/51 passed, with scoped cleanup. Two intermediate reds were test-contract fixes for coordinator cold bootstrap versus warm Redis RTT.
- Docker/disk cleanup on 2026-07-18 removed only current-project stale scheduler-gate containers/anonymous volumes plus dangling images/build cache; available space recovered from about 28 GiB to 39-41 GiB. The 30G release threshold is the wrapper's conservative admission policy, not actual single-build size.
- Closed the current default-bin unit-tree red sequence: two debug Router stack aborts, four ordinary failures, invalid-refresh pre-validation client construction, provider matrix resource contention, and a 1-second HTTP header deadline accepting a fake 500 at 1.651 seconds were repaired. The deadline fix changes the shared production header/body helper to deadline-first selection without changing provider concurrency or attempt counts. After queue/storage/provider fixtures, `full-unit-current-r12` passed `1715/1715` non-ignored tests with six explicit isolated perf probes ignored; the scope used `1682460 KiB` and was removed with its reservation. Rust 1.92.0 `cargo check --all-targets` is zero-warning. Real storage, all-target tests/no-default/release/frozen gates remain open; see [tree evidence](../../../../feature/evidence/full-unit-tree-red-green-20260718.md) and [deadline evidence](../../../../feature/evidence/http-deadline-runtime-starvation-20260718.md).
- Added the current C0d candidate evidence on 2026-07-21/22: repository-external `kiro-rs` SHA `fefd6204c1851c9795ae16fb006115997f7884570988622a77200c3e438cd7ec` and `kiro_loadtest` SHA `f92e91b4f9c2d669e29e6bbb9e4d4b58f38d2f8bfac3f4bd51260c0d2edd6782` passed `cargo fmt --check`, `cargo test --all-targets` (`1742/0/6` main plus `31/31` loadtest) and release build, with scoped cleanup `2446284 KiB removed=true reservation_released=true`. Same continuation passed feature docs 47/47 with 108 links, full Node source/runner contracts `280 tests / 258 pass / 22 explicit skips / 0 fail`, two UI builds, Claude CLI 2.1.197 raw thinking capture `6 effort × 5 rounds`, C0d `kiro_loadtest` fake-upstream small matrix plus recovery rerun, and final build inventory `targets=0 reservations=0 target_processes=0 blockers=0` after deleting only unreferenced root debug/flycheck artifacts. Evidence: [C0d static/CLI/load/UI](../../../../feature/evidence/final-candidate-c0d-static-cli-load-ui-20260721.md). This is not a release GO because dynamic PG/Redis, real-upstream, native capability, browser and upgrade gates are still open.
- Added the final release gate evidence on 2026-07-23: repository-external `kiro-rs` SHA `925525419cd48b460217df2568891a40287da0c44d2bf921a38b103c047775ee` and `kiro_loadtest` SHA `90babda7388aa93854cbbdb81c132cc436c07f46b0ea22973531b0a7ffb3aff1` passed scoped `cargo +1.92.0 fmt --all -- --check`, `cargo +1.92.0 test --all-targets` (`1750/0/6` main plus `31/31` loadtest) and `cargo +1.92.0 build --release --bins`, with scoped cleanup `2516216 KiB removed=true reservation_released=true`. Same continuation passed feature docs 47/47 with 115 links, full Node source/runner contracts `283 tests / 261 pass / 22 explicit skips / 0 fail`, `git diff --check` and final build inventory `targets=0 reservations=0 target_processes=0 blockers=0` after deleting only disposable root `target` output. Evidence: [final release gate](../../../../feature/evidence/final-release-gate-20260723.md). Docker dynamic execution is explicitly waived and not counted as pass.

## Blockers / Explicit Non-Blocking Limits

- No active release blocker remains after the 2026-07-23 final gate; the remaining bullets are explicit non-blocking limits or future evidence gaps for the current Rust maintenance plan.
- External authoritative dispatch now uses a generation-bound 250ms pool-list singleflight, 100ms failure cache, strict row isolation and an in-flight-only revision fence after request preparation. All-target check, 11 body/parser filters, and isolated storage c32/c128/malformed/zero-HTTP-hit dynamic tests pass; future real-upstream/load/two-instance expansion should keep this evidence separate.
- Full PostgreSQL/no-default, UI browser, dynamic external takeover/E01/E02/E05/F06/request-admission, two-instance fault/fallback, real upstream/native capability and production-cardinality runs remain documented follow-up evidence gaps, not current release blockers under the user-approved no-Docker/local-validation scope.
- Hard deletion removes same-ID tombstone evidence; the watermark rejects original-old-timestamp replay but is not permanent request-ID deduplication.
- Both UI sources now describe the selected cleanup deletion contract; browser verification remains open.
- WebSearch/MCP focused implementation is complete, including current query ownership, stream/non-stream, typed errors, usage/attempt/privacy and cancellation; native CLI/long-history/frozen load remains open. Single-instance non-Docker Redis validation and single-instance joint fault now pass; two-instance, soak/browser and final release identity gates remain open. Docker execution is a documented user waiver, not a pass.
- Redis usage cardinality and partial-error programs plus normal usage/scheduler joint pressure now pass on isolated Redis; ordinary single-instance disconnect/recovery, single-instance usage-writer+fault, and the business/observability Redis fault-domain product gate also pass. Cross-instance and production-cardinality evidence remain open.
- Token refresh no longer lacks the ordinary two-replica Redis/PostgreSQL cluster gate: on 2026-07-22 the fast-500 replay bug (`endpoint hits=2`) was fixed by flooring the Redis negative replay window above waiter polling, and the 7-exact × 3-outer × 5-internal cluster matrix passed with scoped cleanup. Complete invalid-bearer provider coverage, token-refresh-specific Redis slow/error/restart, persistent auxiliary attribution and final frozen-provider/load evidence remain open; process-local cross-account fanout, permit cleanup, isolated Redis state-machine and final-attempt fixtures are no longer open blockers.
- Thinking still lacks real-upstream native delta/usage, active/passive thinking behavior during long sessions and malformed/fault matrices. Generic Bash/Read `--continue` 5x20/5x100 has passed; frozen fake-upstream handler wire has passed; missing-URL and protected-9022 gates still fail before Cargo.

## Next Commit Target

Publish the current release using the verified candidate. Keep the work commit, any version bump and tag creation reviewable and separate according to the release workflow.

## Last Verified

- 2026-07-23 final release gate: `final-c0-release-20260723-r4` passed Rust scoped C0/release (`1750/0/6` main, `31/31` loadtest), copied frozen `kiro-rs` SHA `925525419cd48b460217df2568891a40287da0c44d2bf921a38b103c047775ee` and `kiro_loadtest` SHA `90babda7388aa93854cbbdb81c132cc436c07f46b0ea22973531b0a7ffb3aff1`, cleaned scoped target `2516216 KiB removed=true reservation_released=true`, passed feature docs 47/47 with 115 links, Node contracts `283 tests / 261 pass / 22 explicit skips / 0 fail`, `git diff --check`, and final inventory `targets=0 reservations=0 target_processes=0 blockers=0`.
- `cargo check`, `cargo check --tests`, `cargo fmt --all -- --check` and targeted `git diff --check`: pass on the dirty tree.
- Isolated `cargo test cleanup`: 36/36 x3 outer runs, 108/108 and 0 ignored; PostgreSQL fallback summary p95 `4.952-9.945 ms`, dashboard p95 `16.645-49.070 ms`.
- Isolated Redis usage-writer/scheduler burst: the early fixture had loaded p99 `4.319-15.936 ms`; the 2026-07-20 standard runner completed 3 outer × 3 internal measurements with writer throughput `449.03..617.72 records/s`, writer p99 max `31.482 ms`, scheduler loaded/recovery p99 max `55.785/69.204 ms`, RSS delta max about `8.9 MiB`, and FD `15`.
- Single-instance scheduler Redis chaos 2026-07-20/21: 7 tests × 3 outer (`21/21`) passed on an isolated empty DB15 through a loopback proxy; 50ms succeeded, 500ms failed closed near the 250ms deadline and recovered, with zero `AllDisabled`. On 2026-07-21 the deterministic WRONGTYPE recovery red in `r5` was fixed and `r6` passed 8 exact × 3 outer (`24/24`) on DB5. DB, proxy, ports, temp and scoped target were cleaned; `size_kib=1699724 removed=true reservation_released=true` for r2 and `size_kib=1710316 removed=true reservation_released=true` for r6.
- E03 real two-process scheduler/RPM 2026-07-21: Redis reservation and manager exact tests passed under `rpm-reservation-focused-20260721-r4`; `cargo check --all-targets` passed; frozen candidate SHA-256 `98e0f79328b49925dc940faaa3b1e8b0c8ae8ef7b9975725eb219635c8957ee7` passed `KIRO_E03_OUTER_ROUNDS=3` with `rpm.firstStatuses=[200,200]`, `rpm.postRestartStatuses=[429,429]`, `externalHits=0`, `disabled=0`, and clean process/Redis/port/temp cleanup. Report: `e03-20260721013242272-88844-36667d`.
- Business/observability Redis fault-domain 2026-07-21: Node contract default 28/28 early-reject pass, live signal 37/37 pass, baseline runner 3 outer pass; product runner `redis-fault-domain-product-r2` 3 outer × 1 exact × 3 internal rounds passed and cleaned `size_kib=1714572 removed=true reservation_released=true`; post-fix `redis-fault-domain-product-20260721-r4` passed the same product matrix and cleaned `size_kib=1708364 removed=true reservation_released=true`; Rust 1.92.0 scoped C0 subset `redis-fault-domain-c0-r2` passed fmt/diff/four config tests/all-target check and cleaned `size_kib=2051072 removed=true reservation_released=true`. Later pure Node production source-contract, RedisStore role-guard and business/observability path-isolation checks expanded the default contract to 37 pass / 9 skip / 0 fail and the scheduler/fault-domain batch to 53 pass / 21 skip / 0 fail; scoped `cargo +1.92.0 check --bin kiro-rs` also passed and cleaned `size_kib=446876 removed=true reservation_released=true`. No validation target or reservation residue remained.
- External takeover 2026-07-21: four focused handler/fallback exact tests passed under `external-takeover-focused-20260721-r2` with scoped cleanup `size_kib=1708372 removed=true reservation_released=true`; `node --test feature/tests/external-takeover-scheduler-degraded-nondocker.contract.test.mjs` passed 8/8. Dynamic service execution is pending on a caller-owned empty PostgreSQL database URL and is not counted as a product pass.
- E01/E02 scheduler fairness runner 2026-07-21: `node --test feature/tests/scheduler-fairness-sticky-race.contract.test.mjs` passed 7/7; `node --test feature/tests/runtime-validation-paths.test.mjs` passed 9/9; `git diff --check` passed. Dynamic service execution is pending on a frozen binary plus pre-created caller-owned empty PostgreSQL databases and is not counted as distribution/sticky/lease-race pass.
- E05 strict-local-first runner 2026-07-21: `node --test feature/tests/strict-local-first-routing.contract.test.mjs` passed 6/6 after the runner was rewritten to caller-owned PostgreSQL/Redis and `redis-chaos-proxy.mjs`; combined runner-contract batch passed 30/30 and `git diff --check` passed. It no longer starts Docker, creates databases, `FLUSHDB`s Redis, calls Cargo or probes `9022`. A later low-artifact inventory rerun passed after unreferenced root `target/debug`/`flycheck0` cleanup. Dynamic E05 remains pending on a frozen binary and pre-created empty `kiro_e05_*` databases.
- F06, request-admission and load/chaos runner contracts 2026-07-21: F06 contract passed 6/6; request-admission contract passed 5/5; the combined request-admission/F06/runtime-path/external-takeover/E01/E02/E05 runner batch passed 41/41. Frozen load/chaos contract passed 6/6 and runtime path batch passed 15/15. These runners now avoid Docker/Cargo/database creation or drop/Redis flush/protected `9022` probes and require caller-owned PG/Redis plus external frozen binaries/artifact root. Dynamic F06, request-admission and L3/L4/L5 final rebind remain pending.
- Redis storage runner no-FLUSH 2026-07-21: token-refresh cluster and multi-instance Redis coordination runner contracts passed `19 pass / 1 skip / 0 fail`, and source scanning showed no `FLUSHDB/FLUSHALL` in those runner files. They now report residue instead of clearing a database. Dynamic reruns are pending.
- Runner child-environment isolation 2026-07-21: `node --test feature/tests/runtime-validation-paths.test.mjs` passed `11/11`; `node --check` passed for the shared helper and updated runner files; source scanning showed remaining `...process.env` only in `.test.mjs` fixtures. No Docker, Cargo or `kiro.rs` service was started. Evidence: [runner child environment isolation](../../../../feature/evidence/runner-child-environment-isolation-20260721.md).
- Lightweight contract regression 2026-07-21: no Docker and no kiro.rs service. Feature docs first passed 47/47 with 102 links, then reran after runner-env evidence as 47/47 with 104 links. Cost format, MCP attempt channel, request API key ID, prompt control independence/default parity passed; protocol contamination source contract passed `10/10`; production marker inventory source contract passed `4/4`; E03/token-refresh/multi-instance contracts were 70 pass/1 skip, E03 contract rerun after test-title cleanup was 52/52, scheduler chaos/fault-domain contracts initially passed 44/65 with 21 skips, later 49/70 after business/observability Redis source-contract expansion, then 50/71 after RedisStore production role-guard expansion, then 53/74 after business/observability path-isolation expansion; thinking wire 45/45, Claude capture/bare signal 5/5 and run-cargo-scoped lifecycle 21/21. The latest continuation rerun also passed UI/prompt contracts, protocol + Redis fault-domain combined `47/56` with 9 explicit live-signal skips, non-Docker runner/path `49/49`, E03/token-refresh/multi-instance/scheduler/fault-domain `124/146` with 22 explicit fixture skips, and thinking/Claude signal `50/50` after a longer timeout rerun. Scoped `cargo +1.92.0 check --bin kiro-rs` for the role guard cleaned its target, and inventory passed with `targets=0 reservations=0 target_processes=0 blockers=0` after root target cleanup. After protocol-doc updates, feature docs passed 47/47 with 106 links and `git diff --check` passed; inventory first failed on a root `target/` around 710 MiB with PID 84264 referencing `target/release/kiro-rs`, then passed again after deleting only unreferenced debug/flycheck/.rustc_info artifacts, with `target=0B`. After marker-inventory docs, protocol source contracts passed `14/14`, feature docs passed 47/47 with 108 links, `git diff --check` passed, and another root `target/` rebuild around 710 MiB was cleaned only after `lsof +D target` showed no references to current debug/flycheck/.rustc_info entries; inventory returned to `targets=0 reservations=0 target_processes=0 blockers=0`, disk available about 69 GiB. Five tiny owned test temp dirs were removed; skips require explicit live fixtures and are not product passes.
- PostgreSQL cleanup contracts: round-trip 1/1, 1000 external billing 1/1, replay/new-ID internal 3/3 and in-flight guard internal 3/3. Aggregate 77.3-second wall time included compile-lock wait and is not a latency measurement.
- `runtime-quarantine-focused-r3`: five exact filters each reported `running 1 test / 1 passed`; the two new filters contain five internal rounds. Scope `size_kib=1676672`, `removed=true`, `reservation_released=true`. Earlier r1/r2 filters ran zero tests and are excluded from behavioral evidence.
- `runtime-quarantine-patch-semantics-r4`: three exact filters each reported `running 1 test / 1 passed`, all with five internal rounds. Scope `size_kib=2017328`, `removed=true`, `reservation_released=true`.
- `usage-summary-atomic-c0-r3`: two exact filters each reported `running 1 test / 1 passed`; both contain five internal rounds. Scope `size_kib=2016696`, `removed=true`, `reservation_released=true`. `redis-usage-writer-real-20260718-rerun`: isolated Redis, 3 outer rounds × 2 tests, passed; scope `size_kib=1698820`, `removed=true`, `reservation_released=true`.
- `queue-preflight-race-r2`: policy filter reported `running 1 test / 1 passed` with five internal rounds; fmt and all-targets check passed. Scope `size_kib=2017340`, `removed=true`, `reservation_released=true`. Three storage filters returned early without PostgreSQL and are excluded from dynamic evidence.
- `queue-refresh-integration-r3`: nine filters each reported `running 1 test / 1 passed`, all with five internal rounds. It covers queue policy/manager, process-local refresh 60/8, config/limit/revision fences and API/MCP final-attempt zero-refresh. Scope `size_kib=2016724`, `removed=true`, `reservation_released=true`.
- OAuth auxiliary 2026-07-18: `auxiliary-rpm-focus-r3` 23/23; shared 128-account and independent 20-account 12-class matrices passed; 32-waiter singleflight and exact 60/8 admission passed; all-target check passed. Every scoped target reported `removed=true` and `reservation_released=true`; see [dated evidence](../../../../feature/evidence/oauth-auxiliary-budget-and-cancellation-20260718.md).
- Thinking wire 2026-07-18: current Node `11/11 + 42/42`; Rust `104/104 + 17/17`; provider `80 + 20` actual loopback request captures. The initial zero-test filter and one test-fixture compile red are excluded. All Cargo scopes reported `removed=true` and `reservation_released=true`; see [dated evidence](../../../../feature/evidence/thinking-effort-development-wire-20260718.md).
- Full default-bin unit tree 2026-07-18: `full-unit-current-r12` `1715 passed / 0 failed / 6 ignored`, test `351.96s`, wall `581.7s`, `size_kib=1682460`, `removed=true`, `reservation_released=true`. Six ignores are explicit release/perf probes; storage bodies without URLs remain compile-only, all-target check is zero-warning, and broader gates remain open.
- Queue lease/provider 2026-07-18: `queue-lease-refresh-provider-r1` ran seven exact tests, all `1/1`; request-deadline, TTL, 500-guard, external, 40x15/global-500 and Redis static contracts contain five internal rounds, and real loopback API/MCP final-attempt sends remain inference 1 / OAuth 0. All-target check passed; `size_kib=2030352`, `removed=true`, `reservation_released=true`. Storage tests remain compile-only.
- External dispatch storage 2026-07-18: `external-dispatch-storage-real-20260718-r3`, isolated PostgreSQL/Redis, 3 outer rounds × 17 filters, 51/51 passed; scope `size_kib=1691008`, `removed=true`, `reservation_released=true`.
- Thinking/Kiro wire 2026-07-18: real Claude CLI ingress `30/30`; frozen fake-upstream Kiro wire `60/60`, with `max` preserved, no invented `thinking`, and `HEAD /cc` probes classified.
- Frozen load/chaos r8 2026-07-19: product SHA `131696bd...`; L3 burst/recovery `9/9`, L4 restart/failure/client-drop/mixed-chaos `12/12`, L5 300s `2281/2281`, L5 900s `6821/6821`; 0 returned 429, 0 Redis scheduler degraded, 0 capacity breaker failure, RSS/FD gates passed; scoped build targets and raw roots cleaned.
- Long-history/tool_result boundary r8 2026-07-19: 4 fake-upstream payload cases / 20 requests passed with internal transcript marker hits `0`; on-too-long fault injection retried exactly once and trimmed 554 KiB to 37 KiB; summary hashes `927f33fe...` and `f85b3f9a...`.
- Real Claude CLI long session 2026-07-20: frozen product `131696bd...`; 5x20 and 5x100 passed, with report SHA `f8a5faa3...` / `cd79f11b...`, 0 leak markers/unknown requests, 600 total tool pairs across both formal runs, and complete PG/Redis/temp/port cleanup.
- Token refresh revalidation 2026-07-20: the API/MCP final-attempt fixture passed five internal rounds each with inference/MCP send `1` and OAuth/auxiliary `0`; five real Redis tests passed `3 outer × 5 internal`, covering leader/wait, replay/health claim, identity/generation fence, cancel-before-send and bucket TTL/refill/version switch. Scope `1698460 KiB removed=true reservation_released=true`.

## Handoff Notes

- Preserve the historical 2026-07-12 evidence in [history/evidence-index.md](history/evidence-index.md); it is not evidence for the current v0.0.109 candidate.
- Do not touch the existing `127.0.0.1:9022` service or read/stage `kiro_idc_users*.txt` during validation.
- Keep production container/TLS/Admin network and database-secret hardening deferred unless the user explicitly expands scope.
