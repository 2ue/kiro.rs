# Implementation Status

Date: 2026-07-24

## Current Phase

`post-v0.0.115 scheduler-risk hardening in progress`：已完成 114/115 发布后的生产只读复核，并针对“Redis 调度退化 + 快速失败 RPM + 上游 TEMPORARILY_SUSPENDED 后整池烧光”的放大链实现第一批修复。当前工作树尚未发布新版本；下一步是完成隔离负载/异常恢复验证、UI 类型检查、文档检查和发布门禁。

## 2026-07-25 Production follow-up

- 对 `152.53.243.159` 和 `152.53.194.142` 追加只读快照：
  - `.159` 当前仍运行 `0.0.113`，本地 15 个启用凭据全部 `TemporarilySuspended`，最近 30 分钟主要由外部池成功承接；旧缺陷配置仍存在。
  - `.142` 当前运行 `0.0.114`，最近 30 分钟无流量，2 个启用凭据正常；过去 2 小时仍有 18 个 risk suspension/删除事件；外部池全局关闭。
- 2026-07-25 04:54 +08 又补一轮当前态只读复核，证据目录 `tmp/prod-evidence/20260725-044228-rpm-slow-142-159`：
  - `.159` 主机/PG/Redis 当前未打满，入口约 `19~83 rpm`，但外部池成功请求平均/最大耗时高，最大约 `503s`；本地 `TemporarilySuspended=15`，所以当前慢点主要是“本地池不可调度后全走外部池，外部池/上游慢”，不是当前 Redis scheduler CPU 卡死。
  - `.159` 仍大量成功 0 计费，`claude-opus-4-8` 等模型在 `jinnyapi/apiv3.52codeflow/kkkkyue` 上几百条 success 的 `externalPoolBilling.totalCostUsd` 为空或 0；当前分支的 dashed/dotted pricing fix 需要发布后按专题 SQL 复核。
  - `.142` 当前 `healthz/readyz` 正常，Redis `instantaneous_ops_per_sec=0`、`slowlog_len=0`，近 2 小时无普通业务 usage；历史 `sampled request rejection` 是 admission 采样 usage，模型固定 `unknown`、usage 为 0，不按外部池成功计费异常处理。
- 新增 request-entry fast-fail：
  - 在 raw external 接管之后、完整 parse/payload guard 之前，对无外部池接管可能且本地池处于 `NoCredentials/AllDisabled/ProxyBlocked/SchedulerRedisDegraded/RiskCircuitOpen` 的请求快速返回；
  - 使用 `local_pool_route_state_cached`，不新增 scheduler Redis read/probe 热路径；
  - 对临时类状态写入 request-admission per-key local backoff；
  - 不抢跑 `Ready/NoModelCompatible/AllCoolingDown/CapacityFull`，保留模型校验和等待/队列语义。
- 新增/复跑验证：
  - `local_pool_fast_fail_*`: 2 passed；
  - `local_temporary_backoff`: 4 passed；
  - `local_pool_risk_circuit`: 1 passed；
  - `request_admission_has_conservative_defaults_and_explicit_zero_disables`: 1 passed；
  - `provider_status_and_non_eventstream_matrix_is_private_typed_and_bounded`: passed；
  - `handler_binary_eventstream_with_json_content_type_is_body_sniffed_for_five_rounds`: passed；
  - `external_pool_billing_matches_dashed_opus_request_to_dotted_pricing_model`: passed；
  - release C0 final：Rust 1.92.0 scoped `cargo fmt --all -- --check`、`cargo test --locked --all-targets`、`cargo build --release --bins` 全部通过；main `1781 passed / 0 failed / 6 ignored`，`kiro_loadtest 31 passed / 0 failed`；冻结 `kiro-rs` SHA-256 `1290930ed48ee6e20d6ed0ea01095aa08d190164c5252921b7fa1688ca5e569e`，`kiro_loadtest` SHA-256 `4f529fd5484b8f5552c0abb2a47bb5c9197b526c825a43c6d7cc44cd37a7ffd2`；
  - C0 后清理复核：删除无进程引用的当前仓库 root `target/` 约 `926 MiB`，`node feature/tests/inventory-build-artifacts.mjs --gate` 通过；
  - 本地 9022 使用同一冻结候选补 direct/Claude CLI smoke：direct non-stream 第 2 轮成功且 usage 非零；Claude CLI 普通 `stream-json` 成功且 final usage 非零；Claude CLI Bash tool 最小场景成功且 tool_result 配对；direct `thinking.type=adaptive + output_config.effort=max` 成功；上述输出均未命中 `bashHash/readHash/editHash/Tool results provided/function_results/credential/fallback pool/upstream pool/private scheduler/local_scheduler`。
  - 所有本轮 Cargo 批次均通过 `feature/tests/run-cargo-scoped.sh` 执行并清理 scoped target。
- 文档：
  - [生产低并发高 RPM / 前端慢 / 调度卡死排查](issues/prod-scheduler-rpm-freeze-142-159-20260725.md)
  - [Social/Gmail 凭据 api_protocol_error](issues/social-gmail-api-protocol-error.md)
  - [外部池成功请求 0 计费与非流式 usage 捕获分裂](issues/external-pool-success-zero-billing.md)
  - [Release C0 and Claude CLI smoke evidence 2026-07-25](evidence/release-c0-cli-smoke-20260725.md)

## 2026-07-24 Scheduler Risk Follow-up

- 本轮修复：
  - local-pool risk circuit：多个凭据短窗口风险控制后打开本进程账号池熔断，停止继续探测剩余本地账号。
  - circuit 打开后 `local_pool_route_state` / `acquire_context` 先走进程内检查，不先碰 Redis scheduler 热路径。
  - risk circuit public error 归一为临时失败并带 `retry-after`。
  - 单凭据 RPM/并发变化重置该凭据 warmup。
  - 全局 credential RPM、单凭据全局并发、全局 dispatch 并发、weighted capacity、负载均衡模式和 warmup 参数变化时，重置本进程活跃凭据 warmup。
  - Usage realtime 新增 `successRequests/errorRequests/successRpm/errorRpm`，两套 usage 页面显示成功/错误细分。
  - request admission 采样记录文案从 `sampled request rejection` 改为 `request rejected before upstream dispatch`。
  - request admission 新增 per-key 本地临时调度失败 backoff：本地 risk circuit / Redis scheduler degraded / 本地账号调度容量或队列临时失败最终返回下游时，同一 request API key 在 1-8 秒内入口层直接 429，不进入 provider/Redis scheduler 热路径；普通上游 429 不触发。
- 已通过聚焦验证：
  - `local_pool_risk_circuit_stops_burning_remaining_credentials`
  - `priority_mode_respects_warmup_candidate_share`
  - `credential_capacity_updates_reset_warmup_remaining`
  - `runtime_capacity_updates_reset_active_credential_warmup`
  - `auxiliary_focus_attempt_limits_map_to_public_temporary_failure_without_internal_terms`
  - `summary_counts_high_cache_and_sources`
  - `redis_usage_summary_and_dashboard_are_materialized`
  - `non_stream_missing_usage_injects_estimated_billing_body`
  - `stream_missing_usage_builds_estimated_billable_external_pool_billing`
  - `runtime_config_migration`
  - `external_pool_scheduler_redis_fallback_preserves_explicit_boolean_values`
  - `local_pool_circuit`
  - `test_sonnet_high_concurrency_dispatch_respects_limits_and_spreads_load`：600 请求、24 账号、global 48、per credential 3，20 个非冷却账号全部参与，分布 29-31 次。
  - `test_model_scoped_429_high_concurrency_disabled_and_model_filters`
  - request API key admission queue/RPM/concurrency focused tests：single-key RPM、key 隔离并发、bounded queue、FIFO queue。
  - request API key admission local backoff：`local_temporary_backoff_*` 4 项通过，覆盖 RPM 前拒绝、retry-after 上限、排队 waiter 唤醒和 admission disabled；handler 分类器 `local_scheduler_error_enables_admission_backoff_classification` / `upstream_rate_limit_does_not_enable_local_admission_backoff` 通过。
  - `request_admission` 模块回归 24 项通过。
  - `local_pool_risk_circuit_stops_burning_remaining_credentials` 通过。
  - `warmup` 相关聚焦 6 项通过。
  - `summary_counts_high_cache_and_sources` 与 `redis_usage_summary_and_dashboard_are_materialized` 通过。
  - `cargo check --locked --all-targets` 通过。
  - `git diff --check`、`node feature/tests/check-feature-docs.mjs`、`ui pnpm run check`、`admin-ui pnpm exec tsc -b --pretty false` 通过。
  - `node --test feature/tests/*.test.mjs` 通过：283 tests / 261 pass / 22 explicit skips / 0 fail。
  - Claude Code CLI 当前版本记录为 `2.1.197`。本轮未启动真实 CLI temp service：当前 `9022` 为既有本地服务，按验证安全合同不触碰；真实 CLI C1-C4 需要后续冻结临时 binary 和独占端口再跑。
- 构建产物约束：上述 Cargo 批次均通过 `feature/tests/run-cargo-scoped.sh` 执行，日志显示 `removed=true reservation_released=true`。
- 当前 build artifact inventory 的唯一 release-gate blocker 是既有 PID `84264` 仍从仓库 `target/release/kiro-rs` 运行并写 `target/local-verify/kiro-rs-9022.log`；这是用户本地 9022 服务，不属于本轮 scoped Cargo 残留，未停止、未删除。

## 2026-07-23 Final Release Gate Checkpoint

- Git HEAD at validation start: `ac5bc57d5ecf786c7f96d66c1dd7101e2eda9c65` (`ac5bc57`)。
- Final frozen `kiro-rs` SHA-256: `760345d76b3d2ea70694cc420cfde5078ebc8056c7a31a6d7df135d714509839`。
- Final frozen `kiro_loadtest` SHA-256: `3fbaa97a1e0556f38546393068b3afd47caaa48280620c6c8dec3d55d7828ada`。
- Rust gates:
  - no-default `cargo test --locked --all-targets --no-default-features`: main `1757 passed / 0 failed / 6 ignored`，`kiro_loadtest 31/31`；
  - default `cargo test --locked --all-targets`: main `1757 passed / 0 failed / 6 ignored`，`kiro_loadtest 31/31`；
  - `cargo fmt --all -- --check`: pass；
  - clippy baseline: `849 warnings` / baseline allows `849`；
  - release build: pass；
  - all scoped targets cleaned with `removed=true reservation_released=true`。
- Non-Cargo final gates:
  - `node feature/tests/check-feature-docs.mjs`: 48 issue docs / 116 links pass；
  - `node --test feature/tests/*.test.mjs`: `283 tests / 261 pass / 22 explicit skips / 0 fail`；
  - `git diff --check`: pass。
- Real Claude CLI final gates:
  - raw thinking capture `30/30`；
  - Kiro thinking wire `60/60`；
  - bare invoke `20/20`；
  - long-session continue `5 × 20 tool cycles`，`110` CLI turns，`100/100` tool pairs，`leakMatches=0`。
- Frozen load/chaos final gates:
  - L3 burst/recovery `9/9` pass；
  - L4 restart/error/client-drop/mixed chaos `12/12` pass；
  - L5 `60s soak + 60s idle` pass；`120s soak + 15s idle` 仅 RSS cold-start threshold false，请求和恢复均 pass，作为短 idle 观测记录。
- Detailed evidence: [2026-07-23 final release gate](evidence/final-release-gate-20260723.md).

## 2026-07-22 Current Candidate Checkpoint

- Frozen `kiro-rs` SHA-256: `31b8c4749201b0f7666b63a9c268c0b75e21f6c1600b18c77bf39a7c6c249c2e`。
- Claude Code CLI: `2.1.197`。
- Real Claude CLI long session: 5 sessions × 20 tool cycles，110 turns、100 tool pairs、`leakMatches=0`、unknown upstream requests `0`。
- Bare invoke real CLI: 20 cases，15 negative text cases 不升级工具，5 structured cases 正常工具执行。
- Thinking/effort:
  - Claude CLI ingress 30/30，`thinking.type=adaptive`，`max` 保持 `max`；
  - Kiro wire 60/60，CLI/IDE 两入口均把 effort 写入 `additionalModelRequestFields.output_config.effort`，`max` 不截为 `high`，当前 OutputConfig schema 下不发明未广告的 `thinking`。
- Body/reasoning focused Rust: 8 exact tests pass，clean raw body byte-identical，compression bytes/SHA exact。
- Runtime/scheduler/Redis:
  - runtime quarantine storage 18/18；
  - scheduler Redis chaos 24/24；
  - business/observability Redis fault-domain contract 37 pass / 9 skips / 0 fail，product gate 3 outer × 1 exact × 3 internal pass。
  - SchedulerRedisDegraded external takeover：frozen r12 `eca8ce4eb1ebb4c1657d1894dc69d0624313b6ff28e0cba095bf845c0914d13e`，enabled 3 个 clean-DB 轮次通过，每轮 5/5 degraded external 200 + 5/5 recovery local 200；disabled 1 个 clean-DB 轮次通过，5/5 degraded 429 且 local/external hits 0，恢复 5/5 local 200；runner 未用 Docker/Cargo，未探测 `9022`。
- `node feature/tests/check-feature-docs.mjs`: 47 issue docs and 114 relative links pass。
- `node feature/tests/inventory-build-artifacts.mjs --gate`: fail because existing PID 84264 references unmanaged repo `target/` (`target_processes=1`, `blockers=2`). This is not a scoped validation leak and was not deleted.

## Active TODO

1. 执行标准 Git 发布：远端/tag 复核、提交当前修复、升 `Cargo.toml` 到 `0.0.117`、创建并推送 `v0.0.117`。
2. 发布后监控 GitHub Actions。
3. Docker 动态验证按用户当前要求豁免；Docker/依赖验证程序仍必须保留可执行合同，不能记为 pass。
4. 发布后观察生产 recurrence/usage/scheduler 指标，若出现新真实样本，按 `kiro-prod-evidence-audit` 只读采集。

## Done This Phase

- 建立根目录 `feature/` 交付树，迁移旧 `docs/feature` 材料并保留 moved stub/迁移表。
- 建立当前事实矩阵、重新验证矩阵、总体修复与发布计划及 15 个当前专题入口。
- 证明 deep-audit release SHA-256 与当前候选一致。
- `cargo test --all-targets`：1199 + 26 通过；聚焦 sanitizer：17 通过。
- `git diff --check` 通过。
- 当前 `cargo fmt --all -- --check`、`git diff --check` 和 Rust 1.92.0 `cargo check --all-targets` 零 warning；evidence 已有零依赖 quick validation 三轮证据。这些静态/聚焦结果不替代 all-target tests、no-default 或完整发布门禁。
- request API key admission 第一批已实现：digest-only identity、token bucket、response-body permit、有限 queue 和有界状态；14 个聚焦/config 测试与 2 个实际 Router 测试通过，L3/L5 和 usage channel attribution 仍待完成。
- shared inference attempt budget 已覆盖本地/外部/stream/payload/rescue send；400 provider HTTP 矩阵在 1/20/60 账号与 5 轮下通过，确定性 400 不再换号。
- token refresh 专项已记录短 TTL 16 caller/30 sends、timeout 32 caller 放大、旧 invalid-bearer 每请求 force-refresh 以及 2026-07-22 新复现的 cross-instance fast-500 二次 leader 根因；dirty tree 中已有 conditional recovery、typed local/Redis wave、health claim 与默认 60 RPM/8 burst admission。`queue-refresh-integration-r3` 关闭 60/8、limit/config/revision 与 API/MCP final-attempt；`auxiliary-rpm-focus-r3` 为 23/23，128-account shared 与 20-account independent 的 12 类 x c1/c8/c32 x 5 轮、32-waiter singleflight 和 cancel/deadline drop 均通过。2026-07-19/20 的真实隔离 Redis 三轮动态均通过，后一轮还复跑 API/MCP final-attempt。2026-07-22 隔离 PostgreSQL/Redis token-refresh cluster gate 修复前红于 fast 500 `endpoint hits=2`，修复后完整 7-test matrix 默认 3 outer 通过（105 内部轮次），覆盖 rotating/non-rotating success、PG CAS stale fence、fast failure replay、health-claim reclaim、stale leader fence；scoped target `1715296 KiB removed=true reservation_released=true`。完整 invalid-bearer provider/load/chaos、token-refresh-specific Redis slow/error/restart、persistent usage attribution、真实 upstream/native CLI 和 frozen candidate 仍 pending。详见 [独立专题](issues/token-refresh-failure-wave-and-cluster-rpm.md)、[更新 evidence](evidence/token-refresh-failure-wave-and-cluster-rpm-20260717.md) 与 [2026-07-18 evidence](evidence/oauth-auxiliary-budget-and-cancellation-20260718.md)。
- transcript/thinking response suppression 已改为 fail-closed；local/external stream/non-stream 聚焦测试通过，真实 CLI `--continue` 5x20/5x100 已关闭 fake-upstream 长会话子项。2026-07-21 新增 `protocol-contamination-source-contract.test.mjs` 纯 Node 源码合同，单独 `10/10` 通过，与 Redis fault-domain 合批 `56 tests / 47 pass / 9 explicit live-signal skips / 0 fail`；它锁定不信任任意 `Hashxxxxxxxx`、raw clean body 不被清理路径 parse/serialize、signed/redacted 原子策略和污染后无 success terminal。真实 Kiro upstream、native MCP/search/image/agent 与 HTTP contamination retry fault gate 仍待完成。
- remote multimodal 已完成第一批 SSRF/资源边界修复：连接时 DNS 过滤、禁用透明 proxy、source/download/base64/attempt/deadline 与 4 工作流准入；19/19 模块、handlers 90/90、50 次 remote 预拒绝、30 次 inline 对照及四档 clean-text 100 轮 identity 通过，load/CLI image 仍待完成。
- body/payload 共享复核已在一个 disposable debug+release scope 中完成：149 个 debug test functions 与 35 个显式 release probes 全绿，覆盖 payload guard 四档 100 轮 identity、图片/document/schema/tool、五类 Messages + 五类 count_tokens、external raw/normalized、provider 80/80 和 CLI/IDE 四档双模式。scope 约 `2,410,696 KiB`，结束时 `removed=true`、`reservation_released=true` 且路径/PID/reservation 均归零；本轮精确 perf 中段输出被执行器截断，最终冻结 binary 分位、真实 CLI 和 L5 仍待完成。
- Claude CLI thinking/effort 入站已用 2.1.197 完成三次 absent/low/medium/high/xhigh/max 各 5 轮，共 90 个独立 session/90 Messages hits：adaptive 恒存在，absent 默认 high，显式 max 原样发送。runner 的未取消 timeout timer 已在第一轮后修复，后两轮 30 cases wall 15.27/13.48 秒且 child/owned-port/temp 清理全绿；随后发现旧 runner 曾读取既有 9022 listener 作为隔离证明，按当前安全合同作废该隔离证据。2026-07-18 no-probe runner 已重跑 signal gate `9/9` 与完整 capture `30/30`，只记录 `protected9022ProbeSkipped:true`；主动/被动长会话、真实 delta/usage 和真实上游仍待完成。
- thinking final-wire runner 的继承环境、错误 normative oracle、leader-only cleanup、signal race、active socket、direct Cargo output 和报告脱敏问题已修复；当前源码 path/contract `11/11`、lifecycle `42/42`。runner 不再查询现有 9022 listener，只按数值拒绝该端口并验证自身端口释放。2026-07-19 通过当前项目专属隔离 PostgreSQL/Redis 与临时 canonical psql wrapper 执行 frozen runtime 60/60；通用 Bash/Read 长会话已独立通过，真实上游与主动/被动 thinking 长会话仍 pending。
- thinking development wire 已关闭：Rust `thinking 104/104`、`effort 17/17`、provider 字节矩阵 80 次以及 converter-to-provider `max` 最终 wire 20 次通过；最终捕获为 `additionalModelRequestFields.output_config.effort=max`，没有 `max -> high`，fake schema 未声明 `thinking` 时 endpoint 不发明该字段。一次 `running 0 tests` 和一次测试夹具编译红项均已排除并记录；所有 scoped targets 均 `removed=true reservation_released=true`。冻结 handler/真实上游仍阻断发布。
- usage cleanup 一致性组在隔离 PostgreSQL/Redis 为 36/36 x3 外层，即 108/108、0 ignored；soft cleanup 负增量、hard cleanup 不双扣、watermark、legacy cost、duration max、零行定点删除、in-flight commit 和 Redis guarded commit 已有多轮证据。
- usage PostgreSQL writer shared / cleanup exclusive transaction advisory lock 已加入；in-flight commit 竞态测试内部三轮通过。
- post-contract PostgreSQL 聚焦：round-trip 1/1、1000 external billing cleanup 1/1、replay/new-ID 内部 3/3、in-flight commit guard 内部 3/3。
- external pool Redis 聚焦修复与隔离 chaos 已完成：WRONGTYPE 四类各 5 轮，clean/disconnect/restart 与同/跨 manager active fencing 各 5 轮，10k 真实 lease commit-unknown 五轮最终 pending/pool/global 为 0，21-cell 延迟/并发矩阵全部 1000/1000 且恢复 5/5；主 shutdown 已接入 release drain。该结果来自 dirty debug candidate，不替代冻结 release 性能与全树门禁。
- scoped build lifecycle wrapper 已完成 owner-aware target、Git-common atomic reservation、12 GiB batch/20 GiB floor、command PID/start/PGID、不可重入 cleanup、success/failure/single/double-signal cleanup 和 stale reaper。20 路 owner-only 严格 admitted 4/rejected 16；全 PID 压力、SIGKILL owner、unknown command metadata、30 个短命令和后台 descendant 矩阵最终 target/reservation/process/marker 均为零。稳定 SHA-256 为 `2a6f219857197c702d7e4c5f89fb1b66467789c0d51781a9dc728327065c431f`；本批没有运行 Cargo。
- 只读 build inventory 已覆盖 repo/worktree/reservation/显式/private-temp target、相对 binary、cwd/executable/txt 引用、Docker 只读提示和 incomplete fail-closed。clean、分类、相对进程、worktree 排除、缺 lsof 五类各 3 轮通过，fixture 前后 stat 一致且零残留。
- 6 个 runtime/CLI runner 已删除 `target/debug/kiro-rs` fallback 和根 target report/runtime 输出，统一要求仓库外绝对冻结 binary 与 owned artifact root；当前 path 合同覆盖 bare invoke、request-api-key multi-instance、scheduler fairness、strict local-first、AWS region lifecycle 和 thinking wire，并新增禁止探测既有 9022 listener 的静态断言。2026-07-18 扩展 Node 合同集合 `85/85` 通过；完整 runtime 重跑仍是发布门禁。
- 2026-07-21 runner 子进程环境隔离补证：新增 `feature/tests/validation-child-env.mjs`，真实 runtime/Claude/scheduler/fault-domain runner 的服务、Claude、proxy 和 scoped-Cargo 子进程改为白名单环境，不再继承调用方整份 `process.env`。`runtime-validation-paths.test.mjs` 当前为 11/11，通过仓库外 binary/artifact、禁止 direct target、禁止 9022 listener probe、白名单环境不泄漏 `DATABASE_URL`/`REDIS_URL`/Anthropic/OpenAI/API key/任意未显式传入 `KIRO_*`，并静态确认所有非测试 validation runner 无 `...process.env`。该项只关闭验证污染合同，不算任何产品动态门禁。
- 两个 Node load runner 已删除 `9022`/默认 key fallback，要求显式 loopback target/key并拒绝受保护端口；非 loopback 需额外 opt-in。5 组合同测试通过，负载文档已改为 scoped build 后运行仓库外冻结 binary、报告写 owned artifact root；当前 frozen L1 fake-upstream 已执行通过，r8 frozen L3/L4/L5 fake-upstream burst/chaos/soak 也已通过。
- PgSQL runtime mutation backlog 已与 dispatch quarantine 拆分：普通 success/API failure/refresh failure 和自动健康 Patch 待重放不再假禁用健康账号，Disable/调度状态 Patch 继续 fail closed。40 账号 backlog 与 40x15/60 RPM/global-500 两条用例各内部 5 轮通过，后续字段语义补充三条精确测试也各内部 5 轮通过；两次早期空 filter 已明确排除。storage-backed 和冻结候选负载仍待完成。
- finite local/external queue lease 已改为一次 TTL 覆盖本请求最大等待加 60 秒，不再每 waiter 每 20 秒续租；unlimited wait 保留 renewal，长 `WaitForCapacityMax` 按 override 计算。请求 deadline 在 admission 时冻结，runtime config 只影响新请求。500 guard、local/external policy、动态 config 和 40x15/global-500 各内部 5 轮通过；真实 API/MCP final-attempt fixture 也保持每轮 inference=1/OAuth=0。2026-07-19 隔离 PostgreSQL/Redis pool-pressure、22 秒 Redis deadline、degraded waiter 和 cancel release 三轮动态通过，2026-07-20 正常 usage/scheduler burst 与单实例 Redis chaos 也通过。2026-07-21 增加跨实例 RPM 同步预约：Redis storage/manager 两个 exact tests 通过，`cargo check --all-targets` 通过，E03 真实双进程 3 outer pass，三轮 RPM `[200,200] -> [429,429]`、externalHits=0、disabled=0；同日单实例 usage-writer+Redis fault r6 24/24 与 external takeover focused/runner contract 通过；external takeover dynamic、跨实例 fault/fallback、真实上游和分布公平仍 pending。详见 [queue lease evidence](evidence/queue-lease-rpm-amplification-20260718.md)、[scheduler Redis chaos evidence](evidence/scheduler-redis-chaos-nondocker-20260720.md)、[E03 evidence](evidence/e03-real-two-process-scheduler-runner-20260720.md) 与 [external takeover evidence](evidence/external-takeover-scheduler-degraded-20260721.md)。
- Redis usage writer 的 snapshot、aggregate 与 seen 已合并为单 Lua 1 RTT；cache-read exact-token bucket 默认硬上限 4096；64-record batch 改为单共享 deadline、无 waiter fanout。`usage-summary-atomic-c0-r3` 两个精确测试各 `running 1 / passed 1` 且各内部 5 轮，scope `2016696 KiB` 后完整回收。随后使用当前仓库专属隔离 Redis 执行 `redis-usage-writer-real-20260718-rerun`，3 outer rounds × 2 tests，两个真实 Redis 五轮程序全部通过；2026-07-20 `usage-scheduler-quarantine-r1` 又完成正常 writer/scheduler 联合 burst 9 个测量轮，writer p99 最大 `31.482 ms`、scheduler loaded/recovery p99 最大 `55.785/69.204 ms`，RSS/FD 通过。普通单实例 scheduler Redis chaos 21/21 另有独立 pass；2026-07-21 `scheduler-redis-joint-chaos-20260721-r6` 在修复 deterministic Redis response-error commit_unknown 分类后完成 8 exact × 3 outer（24/24）。跨实例 usage/fault 和生产高基数仍 open。
- local preflight/acquire 竞态已移除 250 ms runtime availability hint 的错误策略所有权：static external eligibility 已确认且 capacity fallback 开启时使用 `FailFastOnCapacity`，不再进入本地默认 120 秒队列；policy 与 manager global full/CapacityFull/reselect 均内部 5 轮通过且 queue 0。三个 external storage filter 因缺隔离 PG/Redis 提前返回，只计编译；真实 storage/burst 仍 pending。
- external authoritative dispatch 已从每请求完整 PG list 改为 manager-owned、generation-bound 250ms singleflight，失败负缓存 100ms，caller cancellation 不取消 refresh；list/fence c128 timeout、即时第二波 suppression 和 c128 recovery fixtures 已加入。同 `(pool,revision)` 只合并 in-flight fence且完成结果不缓存，URL/header/body/model prepare 后才 fence/attempt/send。static/full row 对 15 类配置损坏按池 strict fail closed且聚合日志。Rust 1.92.0 all-target check 三次零 warning，11 个无存储 body/parser exact filters 实际通过；使用当前仓库专属隔离 PostgreSQL/Redis 的 `external-dispatch-storage-real-20260718-r3` 已完成 3 outer rounds × 17 filters，即 51/51 通过。两处旧红项均为 cold coordinator bootstrap 被误算成 warm Redis RTT 的测试合同问题，已改为 cold bounded + warm 1/2 RTT。详见 [external dispatch evidence](evidence/external-pool-authoritative-dispatch-20260718.md)。
- 2026-07-18 Docker/磁盘复核确认当前真正的大头是 Docker dangling images/历史可回收资源而非 scoped Cargo target；已删除当前仓库上一轮遗留 scheduler-gate 容器/匿名卷、dangling images 和 builder cache，磁盘可用约 28 GiB → 39-41 GiB。未 prune 命名 volumes、未动用户服务、未清另一个同名项目 `/Users/yuanfeijie/Desktop/project/kiro.rs` 正在使用的 scheduler-gate `*-a1` 容器。`30G` 是 release wrapper 的保守准入阈值，不是实际单次开发编译大小。
- 当前默认 bin 完整单测树已完成新一轮红绿闭环：除两次重型 Router debug stack abort、4 个普通失败、invalid-refresh client 构建放大和 provider matrix 资源峰值外，完整树还证明 1 秒 HTTP header deadline 会在 executor 饥饿时于 1.651 秒接受 500。公共 header/body helper 改为 deadline-first 后，10 轮顺序测试、真实 header/body stall、完整 transport/body matrix `245.74s` 和 `full-unit-current-r11` 全绿；加入 queue/storage/provider fixture 后，当前 `full-unit-current-r12` 为 `1715 passed / 0 failed / 6 explicit perf probes ignored`，测试 `351.96s`、wall `581.7s`、scope `1682460 KiB` 且完整回收。Kiro-RS Tool external projection 保留 `32..=4096`；本地 invalid refresh 五轮为 `2890/117/89/87/87 us` 且 client/admission/send/health 全 0。storage test 缺 URL 的正文早退只算 compile coverage。该结果不替代真实 storage、all-target tests/no-default/release/frozen gates，详见 [完整树](evidence/full-unit-tree-red-green-20260718.md) 与 [deadline 证据](evidence/http-deadline-runtime-starvation-20260718.md)。
- 2026-07-18 provider/MCP transient retry-target guard 已完成：full all-target 开发验证先红于 `provider_transport_and_body_fault_matrix_is_private_typed_and_bounded` 的 `provider_header_timeout` 30 秒超时；修复后瞬态失败写入冷却时只有存在可立即调度的备选凭据才继续重试，否则 typed fail 返回，不再进入下一轮 scheduler acquire 等冷却。复测：Node 合同 `21/21`、runner/path/signal `61/61`、docs/format/prompt/API-key/MCP 合同全绿，`cargo fmt --check + cargo check --all-targets` 通过，focused provider fault matrix `1/1` 通过，完整 all-target development run 为 `1724 passed / 0 failed / 6 ignored` 加 `kiro_loadtest 27/27`；所有 scoped targets `removed=true / reservation_released=true`。本机磁盘不足以满足默认 12 GiB release reservation，Rust 结果使用 7-10 GiB development reservation，不替代最终 C0/release gate。详见 [provider transient evidence](evidence/provider-transient-no-retry-target-20260718.md)。
- 2026-07-19 使用当前仓库专属隔离 PostgreSQL/Redis 完成三组真实 storage gate：token-refresh Redis `3 outer × 5 exact`、Redis usage writer `3 outer × 2 exact`、external dispatch `3 outer × 17 exact` 加 runtime quarantine `3 outer × 6 exact`，合计 90 个 exact invocation，均无 failure/panic。三个 scoped targets 均约 1.69 GiB 并在 wrapper 退出时 `removed=true / reservation_released=true`。随后关闭旧 `kiro_cli_repro` Claude CLI/MCP tmux 验证残留，只删除无进程引用的 root `target/debug`/`flycheck0`，最终 `du -sh target -> 0B` 且 `inventory-build-artifacts --gate` 返回 `targets=0 reservations=0 target_processes=0 blockers=0`。详见 [2026-07-19 storage/artifact evidence](evidence/storage-integration-and-artifact-gate-20260719.md)。
- 2026-07-19 真实 Claude CLI frozen gate 新增红绿闭环：pre-fix frozen `70c9741b...` 在普通 `sonnet` + fake catalog 只广告 `claude-sonnet-4` 时复现 `400 model claude-sonnet-4 does not advertise a native reasoning effort field`；修复 native reasoning schema 缺失时提前 400 后，new frozen runtime `e16df13a...` 通过 bare-invoke CLI 20 cases（15 negative literal XML、5 structured Bash tool loop，25 inference，tool_use/tool_result 各 5，cleanup 全 true）和 thinking wire 60 cases（CLI/IDE × 6 effort × 5，inference 60，unknown/invalid/violations 0，cleanup 全 true）。聚焦 Rust reasoning fallback 两个合同通过；bare-invoke signal 3/3 通过。详见 [2026-07-19 frozen CLI evidence](evidence/frozen-claude-cli-thinking-and-bare-invoke-20260719.md)。
- 2026-07-20 新增真实 Claude CLI `--continue` 长会话门：在 frozen product `131696bd...`、fake Kiro upstream、隔离 PG/Redis 下完成 5x20 与 5x100，分别 110/510 CLI turns、210/1010 inference、100/500 Bash/Read tool pairs，0 internal marker、0 unknown request、所有 session/history/usage/cleanup 断言通过。5x100 单 turn p50/p95/p99/max 为 `403.52/505.70/649.76/837.06 ms`，history `2 -> 404`、body `6293 -> 76289` bytes。详见 [长会话证据](evidence/claude-cli-long-session-continue-20260720.md)；真实 Kiro upstream、thinking 主动/被动长会话、native search/MCP/image/agent 和 fault recovery 仍 pending。
- 2026-07-20/21 正常 usage/scheduler 联合压力、单实例 Redis chaos 与单实例 simultaneous fault 补证：`usage-scheduler-quarantine-r1` 3 outer × 3 internal measurements，全轮通过，writer throughput `449.03..617.72 records/s`、writer p99 最大 `31.482 ms`、scheduler loaded/recovery p99 最大 `55.785/69.204 ms`、RSS 增量最大约 `8.9 MiB`、FD 恒为 `15`；`scheduler-redis-chaos-r2` 7 tests × 3 outer，21/21 通过，50ms 成功、500ms 约 250ms deadline fail-closed 后恢复，0 `AllDisabled`；`scheduler-redis-joint-chaos-20260721-r5` 真实红于 WRONGTYPE recovery，修复 deterministic response-error `commit_unknown=false` 后 `r6` 8 tests × 3 outer，24/24 通过。external takeover focused/runner contract 已通过；两实例 fault/fallback、external takeover dynamic、真实 upstream/UI/release 仍 open。
- 2026-07-21 external takeover 补证：源码复核确认 `fallbackOnSchedulerRedisDegraded`、fresh-state guard 和 external eligibility 的接管路径；`external-takeover-focused-20260721-r2` 四个 handler/fallback exact tests 通过并清理 `1708372 KiB removed=true reservation_released=true`；新增非 Docker `external-takeover-scheduler-degraded-nondocker.mjs`，contract 8/8 通过，覆盖 no Docker/Cargo、no protected `9022` probe、loopback-only PG/Redis、Redis DB1..15 和 caller-owned prefix。动态 service run 仍待 caller-owned empty PostgreSQL database URL。
- 2026-07-21 E01/E02 runner 安全合同补证：`scheduler-fairness-sticky-race.mjs` 已从 Docker-managed PG/Redis 改成 caller-owned PG/Redis；不再 Docker run/exec、不再 `FLUSHDB`、不再 `CREATE DATABASE`，每 case 使用独立 Redis `keyPrefix`。`scheduler-fairness-sticky-race.contract.test.mjs` 7/7、`runtime-validation-paths.test.mjs` 9/9、external takeover contract 8/8 和 `git diff --check` 均通过。动态分布公平仍待冻结 binary + 预创建空 PG databases + Redis prefix。
- 2026-07-21 E05 非 Docker runner 合同：`strict-local-first-routing.mjs` 已改为 caller-owned PostgreSQL/Redis 全矩阵入口，保留 10 类模式，要求仓库外冻结 binary、owned artifact root、`modes × rounds` 个预创建 `kiro_e05_*` database、loopback Redis DB/prefix；不启动 Docker、不创建 database、不 `FLUSHDB`、不调用 Cargo、不探测 `9022`，Redis fault 使用 `redis-chaos-proxy.mjs`，子进程使用最小环境。`strict-local-first-routing.contract.test.mjs` 6/6 通过，同批 runtime path 9/9、external takeover 8/8、E01/E02 7/7，合计 30/30 通过；`git diff --check` 通过。最小环境断言曾红于 `startService` 继承 `process.env`，修复后 E05 contract 6/6。后续清理无引用 root target 后 inventory 已 pass。该项只关闭 runner 合同，不算 E05 动态 PASS。
- 2026-07-21 F06 非 Docker runner 合同：`aws-api-key-region-lifecycle.mjs` 已改为 caller-owned PostgreSQL/Redis 入口，要求仓库外冻结 binary、owned artifact root、`kiro_f06_*` PostgreSQL database、loopback Redis DB1..15 和 caller-owned Redis prefix；不启动 Docker、不创建 database、不 `FLUSHDB`、不调用 Cargo、不探测 `9022`，服务子进程使用最小环境，Redis 只清理 owned prefix。`aws-api-key-region-lifecycle.contract.test.mjs` 6/6 通过，同批 runtime path、external takeover、E01/E02、E05 runner contract 合计 36/36 通过；`git diff --check` 通过。该项只关闭 runner 合同，不算 F06 动态 PASS。
- 2026-07-21 Request API Key Admission 非 Docker runner 合同：`request-api-key-admission-multi-instance.mjs` 已改为 caller-owned PostgreSQL/Redis + 本地 `redis-chaos-proxy.mjs` 入口，要求仓库外冻结 binary、owned artifact root、`kiro_request_admission_*` PostgreSQL database 列表、loopback Redis DB1..15 和 caller-owned Redis prefix；不启动 Docker、不创建 database、不 `FLUSHDB`、不调用 Cargo、不使用 `host.docker.internal`、不探测 `9022`，两个服务实例子进程使用最小环境，Redis 只清理 owned prefix。`request-api-key-admission-multi-instance.contract.test.mjs` 5/5 通过，同批 request-admission/F06/runtime path/external takeover/E01/E02/E05 runner contract 合计 41/41 通过；`git diff --check` 通过。该项只关闭 runner 合同，不算 request-admission 动态 PASS。
- 2026-07-21 Frozen load/chaos 非 Docker runner 合同：`frozen-load-chaos-runner.mjs` 已改为 caller-owned PostgreSQL/Redis 入口，要求仓库外冻结 `kiro-rs` 与 `kiro_loadtest`、owned artifact root、tier 对应数量的预创建 `kiro_load_chaos_*` database、loopback Redis DB1..15 和 caller-owned prefix；不启动 Docker、不创建/drop database、不 `FLUSHDB`、不调用 Cargo、不探测 `9022`，fake/proxy/loadtest 子进程使用最小环境，Redis 只 SCAN/DEL owned prefix。contract 6/6 通过，同批 runtime path 合同 15/15 通过；`git diff --check` 通过。该项只关闭 runner 合同；最终 frozen L3/L4/L5 dynamic 仍需用改造后的 runner 重跑。
- 2026-07-21 Redis storage runner no-FLUSH 合同：`run-token-refresh-cluster-validation.mjs` 与 `run-multi-instance-redis-coordination-validation.mjs` 已去掉 runner 级 `FLUSHDB` 兜底；启动前仍要求 caller-confirmed empty DB1..15，测试后若有残留只报告 `residualKeyCount` 并让门禁失败，不清空整库。纯 Node 合同合批为 `19 pass / 1 skip / 0 fail`，skip 仅为显式 live nonempty Redis opt-in；source 扫描确认无 `FLUSHDB/FLUSHALL`。该项只关闭 runner 清理安全合同，不算 token-refresh cluster 或 multi-instance Redis dynamic PASS。
- 2026-07-21 runner child environment isolation：`validation-child-env.mjs` 统一真实 runner 子进程环境白名单；`runtime-validation-paths.test.mjs` 当前 11/11，`node --check` 覆盖 8 个更新 runner，source scan 只在 `.test.mjs` fixture launcher 中保留 `...process.env`。本轮不启动 Docker/Cargo/kiro.rs service，只关闭 validation contamination 合同；动态产品门禁仍 open。详见 [证据](evidence/runner-child-environment-isolation-20260721.md)。
- 2026-07-21 低产物合同回归：主体不启动 Docker、不启动 kiro.rs 服务；除 RedisStore production role guard 的单独 scoped `cargo +1.92.0 check --bin kiro-rs` 外，合同回归本身不触发 Cargo。feature docs 初始 47/47 与 102 links，通过 runner env 补证后复跑为 47/47 与 104 links；cost format、MCP attempt channel、request API key ID、prompt control independence/default parity 均通过；E03/token-refresh/multi-instance runner contracts 70 pass/1 skip，旧标题清理后 E03 contract 单独复跑 52/52，scheduler chaos/fault-domain contracts 初始 44 pass/21 skip、业务/观测 Redis 源码合同补证后合批 49 pass/21 skip/0 fail、RedisStore production role guard 合同后合批 50 pass/21 skip/0 fail、主/观测 Redis 路径隔离合同后合批 53 pass/21 skip/0 fail，thinking wire 45/45，Claude capture/bare signal 5/5，run-cargo-scoped lifecycle 21/21。最新 non-Docker runner/path 合批为 68 pass/1 skip/0 fail，skip 均为显式 live fixture opt-in，不计产品 pass。清理无引用可再生 `target/debug`、`target/flycheck0` 和 `.rustc_info.json` 后，`find . -maxdepth 3 -type d -name target` 无输出，inventory 为 `targets=0 reservations=0 target_processes=0 blockers=0`；Docker 只读盘点超时仍是 manual-only hint。磁盘可用约 71-73GiB，本轮无新增 scoped target/reservation 残留。
- 2026-07-21 继续低产物协议/runner/signal 复核：不启动 Docker、不启动 kiro.rs、不调用 Cargo。UI/prompt 合同（cost format、MCP attempt channel、request API key ID、prompt control independence、prompt default parity）全部 PASS；新增 protocol contamination source contract `10/10` PASS，并与 Redis fault-domain 合批 `56 tests / 47 pass / 9 explicit live-signal skips / 0 fail`；新增 protocol marker inventory source contract `4/4` PASS，锁定生产 marker 允许位置、inert dot placeholder、无 bare `<invoke>` 生产字面量、无旧 `[previous output]`/`[trimmed output]`/`[duplicate output]` 生产生成点、无 invalid tool_result textification；non-Docker runner/path 合批 49/49；E03/token-refresh/multi-instance/scheduler/fault-domain 合批 146 tests：124 pass、22 explicit fixture skips、0 fail；thinking/Claude signal 合批首次 30 秒工具超时截断正在通过的长信号矩阵，随后以 120 秒上限完整复跑 50/50 pass。复跑后无相关测试进程残留，只清理 5 个明确本轮测试前缀的小临时目录，总量约 4 KiB；inventory 仍为 `targets=0 reservations=0 target_processes=0 blockers=0`。详见 [轻量证据第 6 节](evidence/lightweight-contract-regression-20260721.md)、[协议污染源码合同证据](evidence/protocol-contamination-source-contract-20260721.md) 和 [生产 marker inventory 合同证据](evidence/protocol-marker-inventory-source-contract-20260721.md)。
- 2026-07-21 文档补证后复查 build inventory：首次 inventory 因 root `target/` 约 `710 MiB` 且 PID `84264` 引用 `./target/release/kiro-rs` 及其本地 log 而 fail；只检查并删除无引用的 `target/debug`、`target/flycheck0` 和 `target/.rustc_info.json` 后，`target/` 回到 `0B`，inventory 复核为 `targets=0 reservations=0 target_processes=0 blockers=0`。未停止 PID `84264`，未清不确定用户服务资产。详见 [轻量证据第 7 节](evidence/lightweight-contract-regression-20260721.md)。
- 2026-07-21 生产 marker inventory 补证后复跑：protocol contamination + marker inventory source contracts 合批 `14/14` PASS；feature docs `47/47` 与 `108` relative links PASS；`git diff --check` PASS。随后 root `target/` 又被 rust-analyzer/flycheck 类任务重建约 `710 MiB`，`lsof +D target` 对当前可见 debug/flycheck/.rustc_info 无打开引用；只删除这些可再生产物后 inventory 重新为 `targets=0 reservations=0 target_processes=0 blockers=0`，磁盘可用约 `69 GiB`。详见 [轻量证据第 8 节](evidence/lightweight-contract-regression-20260721.md)。
- 2026-07-21 业务 Redis 与观测 Redis 故障域产品 gate 通过：纯 Node contract 初始 28/28 早拒绝 pass（9 live signal skipped）、live signal 37/37 pass、基础 runner 3 outer pass；产品 runner `redis-fault-domain-product-r2` 使用当前项目业务 Redis `26379/15` 与观测 Redis `50892/15`，完成 3 outer × 1 exact × 内部 3 轮，3/3 exact invocations passed，证明 observability latency/disconnect 不影响 business scheduler，business fault fail closed 且不伪装 `AllDisabled`，observability 不回落 business Redis；scope `1714572 KiB removed=true reservation_released=true`。scheduler Redis failure classification 修复后又跑 `redis-fault-domain-product-20260721-r4`，业务 `26379/8`、观测 `50892/2`，3 outer × 1 exact × 内部 3 轮通过，scope `1708364 KiB removed=true reservation_released=true`。Rust 1.92.0 scoped C0 子集 `redis-fault-domain-c0-r2` 通过 fmt/diff/4 个 observability config tests/`cargo check --all-targets`，scope `2051072 KiB removed=true reservation_released=true`。后续新增生产源码合同、RedisStore production role guard 和主/观测 Redis 路径隔离合同后，默认 Node contract 为 46 tests：37 pass、9 live signal skips、0 fail，锁定 `main.rs` usage/Admin 注入、scheduler/external/runtime-event/health business Redis 专用路径、observability Redis 启动失败不回落 business Redis、`UsageRecorder` observability role 断言、主请求路径仅 enqueue 观测 writer 且压力丢弃 summary、`AdminService` cache/cleanup wiring、`RedisStoreRole`、usage materialization entrypoint guard 与 config env/authority guard；`redis-observability-role-guard-20260721` scoped `cargo +1.92.0 check --bin kiro-rs` 通过，scope `446876 KiB removed=true reservation_released=true`；最新 scheduler/fault-domain 合批为 74 tests：53 pass、21 skip、0 fail。一次默认 Rust 1.86 C0 红项按无效门禁登记，不作为源码失败。详见 [证据](evidence/business-observability-redis-fault-domain-20260721.md)。
- 2026-07-19 frozen loadtest L1 fake-upstream gate 已关闭当前 frozen smoke：product binary `e16df13a...` 与 patched `kiro_loadtest` `23c04221...` 执行 `l1_fake_upstream_12_case_matrix_r3`，normal stream/non-stream、thinking stream、tool use、slow first text、slow thinking、JSON exception 200、429、500、invalid tool format、malformed SSE、client drop 共 12/12 通过。前置红项定位为 loadtest 夹具未模拟 Kiro ListAvailableModels/CLI EventStream/native `output_config.effort` 等，并已用 scoped Rust tests 5/5 关闭。随后 r8 frozen candidate `131696bd...` 通过 L3 burst/recovery 9/9、L4 restart/failure/client-drop/mixed-chaos 12/12、L5 300 秒 2281/2281 和 L5 900 秒 6821/6821；L5 raw log 为 0 Redis degraded、0 capacity breaker failure、0 429，RSS/FD gate 通过。该证据不替代两实例、真实上游、长会话、UI、upgrade 和最终 C0。详见 [2026-07-19 frozen loadtest evidence](evidence/frozen-loadtest-l1-fake-upstream-20260719.md) 与 [L3-L5 evidence](evidence/frozen-loadtest-l3-l5-chaos-20260719.md)。
- 2026-07-21/22 C0d current final-candidate static/build/UI/loadtest-tooling gate completed on repository-external binaries: `kiro-rs` SHA `fefd6204c1851c9795ae16fb006115997f7884570988622a77200c3e438cd7ec`, `kiro_loadtest` SHA `f92e91b4f9c2d669e29e6bbb9e4d4b58f38d2f8bfac3f4bd51260c0d2edd6782`. Scoped C0d ran `cargo fmt --check`, `cargo test --all-targets` (`1742/0/6` main + `31/31` loadtest), `cargo build --release --bins`, and cleaned `size_kib=2446284 removed=true reservation_released=true`. Same continuation passed feature docs 47/47 with 108 links, UI/cost/MCP/request-key/prompt contracts, full Node source/runner contracts `280 tests / 258 pass / 22 explicit skips / 0 fail`, Claude CLI 2.1.197 raw thinking capture `6 effort × 5 rounds`, both UI builds, and C0d `kiro_loadtest` fake-upstream small matrix plus recovery rerun. Root target cleanup removed only unreferenced debug/flycheck/.rustc_info artifacts, did not stop PID 84264, and final inventory returned `targets=0 reservations=0 target_processes=0 blockers=0`. Evidence: [C0d static/CLI/load/UI](evidence/final-candidate-c0d-static-cli-load-ui-20260721.md). This does not close PG/Redis service dynamic, real-upstream, native MCP/search/image/agent, browser, upgrade or release gates.
- 2026-07-19 追加 r8 frozen 长历史/tool_result capture：preemptive large-tool-results、mixed-pathological、schema-key-mapping 与 on-too-long large-tool-results 共 20/20 请求成功，captured Kiro text fields 中内部 transcript marker 为 0，tool_use/tool_result 无 orphan。另用 fake upstream 首次返回 `Input is too long` 证明 on-too-long 路径 exactly 2 inference sends，首发约 554 KiB，retry 约 37 KiB。2026-07-20 又完成真实 CLI `--continue` 5x20/5x100 长会话，补齐 session/history/tool-result pairing 和 Hash 映射往返。详见 [长历史 tool_result 边界证据](evidence/long-history-tool-result-boundary-20260719.md) 与 [CLI 长会话证据](evidence/claude-cli-long-session-continue-20260720.md)。

## Blockers

- same-ID 产品合同已定为 soft tombstone 存在期间不复活、cutoff 后新 ID 可写；hard cleanup 后不承诺永久 ID 防重。当前源码与 round-trip 1/1、默认 bin 完整单测树和 all-target check 通过，仍缺 all-target tests/no-default 与完整 storage 套件。
- 两套 UI 源码已同步 soft cleanup 扣统计/费用/Dashboard、hard cleanup 只删 tombstone；浏览器证据未完成。
- 其余阻断项仍是尚未完成的实现/验证门禁，不是外部输入阻塞。
- 生产 recurrence 需要后续只读取证，但本地修复与绝大多数故障矩阵可在假上游/隔离 Redis/PG 下完成。
- external coordinator 当前只支持同一 Redis prefix 对应同一 PostgreSQL authority；不同 PG database/schema 共用同 prefix 已复现约 34 秒 recovering，必须由启动校验拒绝或在 Redis CAS authority 设计后再支持。debug 21-cell 的零延迟聚合 c320 p95 为 1.232 秒，release-mode 性能仍是阻断项。
- external static eligibility 的 SWR、body-mode parity、authoritative selection singleflight 与 prepare 后 revision fence 已在源码完成；真实隔离 PG/Redis 的 PG lock/c32/c128/cross-instance/坏行正文已在 2026-07-18 与 2026-07-19 两批完成。仍未关闭的是 frozen release、两实例、真实负载/chaos、RSS/FD 和生产 recurrence。
- token refresh 仍缺 revision-only same-token、non-rotating refresh-token PostgreSQL CAS fence、leader send 前 PgSQL 权威复核、完整 API/MCP invalid-bearer、两实例 Redis/PG、refresh-specific slow/error/restart、精确 1/60 provider cells 和 persistent usage attribution；process-local 取消、20/128-account fan-out、32-waiter、60/8、真实隔离 Redis health-claim/cancellation bucket 程序、API/MCP final-attempt 和普通 scheduler 单实例 Redis chaos 均已关闭，不再沿用“最近容量 red”。发布保持 `NO-GO`。
- 生产新证据显示 Redis degraded 后又出现短时 `local_all_disabled`。源码确认的 PgSQL runtime mutation 假 quarantine 已完成拆分；finite queue 的每 waiter 周期续租放大也已修复，并已有 40x15/global-500、500 guard、deadline-config 聚焦五轮证据，以及 2026-07-19 真实隔离 PostgreSQL pool pressure / Redis deadline / degraded / cancel 三轮动态证据。2026-07-20 正常 usage/scheduler 联合压力 9 个测量轮和单实例 Redis latency/disconnect chaos 21/21 已通过；2026-07-21 E03 两进程 shared RPM/lease/restart 3 outer 已通过，单实例 usage-writer+Redis fault r6 24/24 也已通过；external takeover focused/runner contract 已通过但动态服务验证仍未执行。剩余是 external 接管动态、两实例 usage/fault 组合和真实上游/Claude CLI 长流与工具场景。详见 [独立专题](issues/high-concurrency-low-rpm-runtime-quarantine.md)。
- Redis usage writer 的 1 RTT/基数/fanout 聚焦合同、当前仓库隔离 Redis 的 cache-read cap / partial-error 动态程序和 2026-07-20 正常 usage/scheduler 联合压力均已通过；普通单实例 Redis disconnect/recovery 有 21/21 chaos pass；2026-07-21 单实例 usage-writer+Redis fault r6 为 24/24；业务/观测 Redis 独立故障域产品 gate 也在修复前后均通过。跨实例 usage/fault 和生产高基数 p95/p99 仍是发布阻断。详见 [独立专题](issues/redis-usage-writer-atomicity-cardinality-and-scheduler-isolation.md) 与 [业务/观测 Redis 故障域](issues/business-observability-redis-fault-domain.md)。
- Usage same-ID per-ID lock 与 service/maintenance lifecycle fence 只对当前协议实例成立；最终发布必须采用全实例 drain/stop 后统一切换，不能 mixed-version rolling。watermark 250ms timeout、lifecycle fence 和六 authority oracle 仍待动态关闭。
- Dashboard `p95DurationMs` 的 MAX 冒充 P95 及 partial-hour 窗口人口漂移是 2026-07-16 旧构建的历史红证据；当前源码已把 `/api/admin/usage-dashboard` 与 `/api/admin/usage-dashboard/windows` 收敛到 PgSQL-only weighted nearest-rank P95，Redis `duration_ms_max -> p95_duration_ms` 仅保留在 helper/test 路径。Runtime/browser/perf 复核与查询性能门禁仍需在隔离 PG/Redis 上完成。
- 构建生命周期当前新增状态：后续 frozen loadtest 的 scoped Cargo 批次均 `removed=true / reservation_released=true`，无 validation scope 残留；本轮发现 rust-analyzer/flycheck 重建的根 `target/debug`/`flycheck0` 约 710 MiB，在 `lsof` 无引用后已按用户要求清理，`target/` 当前不存在，inventory 为 `targets=0 reservations=0 target_processes=0 blockers=0`。最终发布 gate 尚未关闭；后续 frozen CLI/load/UI/upgrade 批次必须各自重跑 reap/inventory，且除非用户授权不得停止用户服务或删除其占用资产。

## Next Commit Target

先完成 cleanup full/writer-performance/chaos/browser 门禁，再完成剩余 P0/P1 与统一候选门禁；不得把版本 bump 混入工作提交。

## Last Verified

- HEAD: `401473c` (`v0.0.109`)，工作树有未提交功能、文档和 skill 变更。
- C0d final candidate 2026-07-21/22：`kiro-rs` SHA `fefd6204c1851c9795ae16fb006115997f7884570988622a77200c3e438cd7ec`，`kiro_loadtest` SHA `f92e91b4f9c2d669e29e6bbb9e4d4b58f38d2f8bfac3f4bd51260c0d2edd6782`。`cargo fmt --check`、`cargo test --all-targets`（main `1742 passed / 0 failed / 6 ignored`，`kiro_loadtest 31/31`）和 `cargo build --release --bins` 通过；C0 log SHA `89bc66f8f262b08b1322baf47b91c9a733b2b5eec53b686cac13755ef109435e`；scoped target `2446284 KiB removed=true reservation_released=true`。同轮静态/Node/UI/CLI-ingress/loadtest-tooling 与 inventory 结果见 [C0d evidence](evidence/final-candidate-c0d-static-cli-load-ui-20260721.md)。该候选尚未完成 PG/Redis dynamic、real-upstream、native capability、browser、upgrade 和 release gates。
- 历史 deep-audit release binary SHA-256: `4623cdf4e3f7bc0e2fe3defa4e0862237e1f64bccbb91671c134e0d6b51556d8`；它不是当前 dirty tree 的冻结候选。
- cleanup：隔离 PostgreSQL/Redis 36/36 x3 外层（108/108、0 ignored）；fallback summary p95 `4.952-9.945 ms`、dashboard p95 `16.645-49.070 ms`；早期 Redis burst scheduler p99 `4.319-15.936 ms`。2026-07-20 标准联合 runner 又完成 9 个测量轮，scheduler loaded/recovery p99 最大 `55.785/69.204 ms`，均低于当前 250ms capacity bound。
- cleanup post-contract focused run：round-trip 1/1、1000 external billing 1/1、replay/new-ID 内部 3/3、in-flight commit guard 内部 3/3；77.3 秒 wall time 含编译锁等待，不作性能结论。
- Redis usage writer：`usage-summary-atomic-c0-r2` all-target check 通过并清理；最终 1 RTT 版本在 `usage-summary-atomic-c0-r3` 的提交顺序和 batch deadline 两个精确测试各内部 5 轮通过，`removed=true / reservation_released=true`。`redis-usage-writer-real-20260718-rerun` 使用当前仓库隔离 Redis，3 outer rounds × 2 tests，各测试内部 5 轮，通过且 `size_kib=1698820 removed=true reservation_released=true`。2026-07-19 又使用当前仓库隔离 Redis 重跑 3 outer rounds × 2 tests，通过且 `size_kib=1691768 removed=true reservation_released=true`。2026-07-20 `usage-scheduler-quarantine-r1` 3 outer × 3 internal measurements 全过，writer throughput `449.03..617.72 records/s`、writer p99 最大 `31.482 ms`、RSS 增量最大约 `8.9 MiB`、FD 恒为 `15`，scope `1698452 KiB removed=true reservation_released=true`。2026-07-21 `scheduler-redis-joint-chaos-20260721-r6` 覆盖 usage-writer 与 Redis fault 同窗，24/24 通过，scope `1710316 KiB removed=true reservation_released=true`。
- Scheduler Redis chaos 2026-07-20/21：`scheduler-redis-chaos-r2` 使用独占空 DB15 与 loopback proxy，7 tests × 3 outer 为 21/21；覆盖 50/500ms latency、breaker threshold/backoff、disconnect/reconnect、300 lease release、cancel 和 commit-unknown，0 `AllDisabled`，DB/代理/端口/temp/scoped target 全清理，scope `1699724 KiB removed=true reservation_released=true`。`scheduler-redis-joint-chaos-20260721-r5` 先红于 deterministic WRONGTYPE recovery，修复 commit_unknown 分类后 `r6` 使用 DB5 8 exact × 3 outer 为 24/24；cleanup `databaseEmpty=true childGroupsStopped=true portsReleased=true tempRemoved=true`，scoped `size_kib=1710316 removed=true reservation_released=true`。
- E03 真实双进程 scheduler 2026-07-21：`rpm-reservation-focused-20260721-r4` 中 storage `redis_scheduler_cooldown_and_rate_limit_round_trip` 与 manager `redis_backed_rpm_reservation_blocks_third_cross_instance_selection` 均 1/1 通过，`cargo check --all-targets` 通过；随后用冻结候选 `/tmp/kiro-e03-candidate.T2iG7N/kiro-rs` 完成 3 outer runtime pass，`rpm.firstStatuses=[200,200]`、`rpm.postRestartStatuses=[429,429]`、`externalHits=0`、`disabled=0`，report `e03-20260721013242272-88844-36667d`。详见 [E03 evidence](evidence/e03-real-two-process-scheduler-runner-20260720.md)。
- Business/observability Redis fault-domain 2026-07-21：Node contract 初始 28/28 早拒绝 pass、live signal 37/37 pass、基础 runner 3 outer pass；产品 runner `redis-fault-domain-product-r2` 3 outer × 1 exact × 内部 3 轮，3/3 exact invocations passed，scope `1714572 KiB removed=true reservation_released=true`；post-fix `redis-fault-domain-product-20260721-r4` 也完成 3 outer × 1 exact × 内部 3 轮并通过，scope `1708364 KiB removed=true reservation_released=true`；Rust 1.92.0 C0 子集 `redis-fault-domain-c0-r2` 通过 fmt/diff/4 config tests/all-target check，scope `2051072 KiB removed=true reservation_released=true`。源码合同、RedisStore production role guard 和主/观测 Redis 路径隔离合同补证后默认 Node contract 为 37 pass/9 skip/0 fail；合批 scheduler/fault-domain 为 53 pass/21 skip/0 fail；新增 scoped `cargo +1.92.0 check --bin kiro-rs` 通过，scope `446876 KiB removed=true reservation_released=true`。无 `.validation-build-*` 或 reservation 残留；后续 root target cleanup 后 inventory 为 `targets=0 reservations=0 target_processes=0 blockers=0`。
- External takeover 2026-07-21：`external-takeover-focused-20260721-r2` 四个 exact handler/fallback tests 通过；scoped cleanup `size_kib=1708372 removed=true reservation_released=true`。`node --test feature/tests/external-takeover-scheduler-degraded-nondocker.contract.test.mjs` 为 8/8，通过 no Docker/Cargo、no protected `9022` probe、DB0/非 loopback/unsafe prefix 预拒绝和 validate-only 合同。动态 service run 未执行，原因是本轮没有可确认独占空 PostgreSQL database URL；不计为 external takeover 产品 PASS。
- E01/E02 scheduler fairness runner contract 2026-07-21：`node --test feature/tests/scheduler-fairness-sticky-race.contract.test.mjs` 为 7/7，`node --test feature/tests/runtime-validation-paths.test.mjs` 为 9/9，同批 `git diff --check` 通过；源码扫描确认 runner 不调用 Docker/Cargo。动态 E01/E02 未执行，不计为分布公平 PASS。
- Strict local-first legacy runner guard 2026-07-21：`node --test feature/tests/strict-local-first-routing.contract.test.mjs` 为 3/3；默认拒绝旧 Docker/Toxiproxy runner。E05 全矩阵动态仍 pending。
- build lifecycle：wrapper success `0`、failure `23`、INT/TERM/HUP `130/143/129` 均清理；双信号、SIGKILL command owner、unknown metadata、owner-only/all-PID 20-way 和 descendant 均通过；inventory 五类三轮矩阵通过。2026-07-19 曾关闭旧 `kiro_cli_repro` Claude CLI/MCP tmux 验证残留并取得 `targets=0 reservations=0 target_processes=0 blockers=0`；2026-07-21 本轮又在无 `lsof` 引用时删除 root `target/debug`/`flycheck0`，复核 `targets=0 reservations=0 target_processes=0 blockers=0`。这只是当前文件系统零残留，不替代最终 frozen 候选后的 release inventory。
- Storage integration 2026-07-19：token-refresh Redis `3 outer × 5 exact`，scope `1690676 KiB`；Redis usage writer `3 outer × 2 exact`，scope `1691768 KiB`；external dispatch + runtime quarantine storage suite `3 outer × 17 exact + 3 outer × 6 exact`，scope `1690164 KiB`；三组均通过且 wrapper 清理完成。原始日志 SHA-256 见 [storage/artifact evidence](evidence/storage-integration-and-artifact-gate-20260719.md)。
- Token refresh revalidation 2026-07-20：API/MCP final-attempt fixture 内部各 5 轮通过；五个真实 Redis tests 各 `3 outer × 5 internal` 通过。scope `1698460 KiB removed=true reservation_released=true`，详见 [updated evidence](evidence/token-refresh-failure-wave-and-cluster-rpm-20260717.md)。
- 发现一次旧 thinking capture runner 读取了 `127.0.0.1:9022` listener 状态；该 runner 已修复为不读取既有 9022，只按数值拒绝该端口并用 `protected9022ProbeSkipped:true` 记录。后续当前 runner 的 signal `9/9` 与完整 capture `30/30` 已按 no-probe 合同通过。未读取或暂存 `kiro_idc_users*.txt`。
- OAuth auxiliary 2026-07-18：首次 `auxiliary-rpm-focus-r1` 为 21/22 并暴露取消 permit 延迟释放；结构化取消修复后 `auxiliary-rpm-focus-r3` 为 23/23。`oauth-shared-burst-r4`、`oauth-independent-burst-r1`、`oauth-singleflight-wave-r1`、默认 60/8 和 `cargo check --all-targets` 均通过；每个 scope 都是 `removed=true / reservation_released=true`。
- 完整默认 bin 单测 2026-07-18：`r9` 保留不可诊断红，`r10` 为 `1705/1/6` 并精确证明迟到 500；deadline-first 后 `r11` 为 `1708/0/6`，当前 `full-unit-current-r12` 为 `1715 passed / 0 failed / 6 ignored`，测试 `351.96s`、wall `581.7s`。当前 scope `size_kib=1682460 removed=true reservation_released=true`，无 scoped target/临时报告残留；Rust 1.92.0 all-target check 已零 warning，更广门禁仍 open。
- Provider transient retry-target 2026-07-18：一次 full all-target development run 先红 `1723/1/6`，失败为 provider header-timeout fault matrix 30 秒超时；修复后 `cargo fmt --check + cargo check --all-targets` 通过，focused fault matrix `1/1` 通过，full `cargo test --all-targets` 为 `1724/0/6`，`kiro_loadtest` 为 `27/27`。对应 scopes 均清理；本机默认 12 GiB reservation admission 因磁盘不足未满足，已按 development reservation 记录为非 release gate。
- Queue/refresh provider 2026-07-18：`queue-lease-refresh-provider-r1` 的 7 个精确过滤器均实际 `running 1 / passed 1`；deadline config、TTL policy、500 guard、external policy、40x15/global-500 与 Redis secret-free 合同各内部 5 轮，API/MCP fake HTTP 也内部 5 轮。`cargo check --all-targets` 无 warning；scope `2030352 KiB removed=true reservation_released=true`。2026-07-19 runtime quarantine storage suite 又用真实隔离 PostgreSQL/Redis 对 pool pressure、pending replay、generation fence、Redis queue deadline、degraded waiter 和 cancel release 执行 `3 outer × 6 exact` 并通过；冻结负载仍未执行。
- Storage validation wrapper protected-port gate 2026-07-18：`run-token-refresh-redis-validation.sh`、`run-redis-usage-writer-validation.sh`、`run-runtime-quarantine-storage-validation.sh`、`run-external-dispatch-storage-validation.sh` 均 `bash -n` 通过；Redis 或 PostgreSQL URL 指向 `127.0.0.1:9022` 时均在 Cargo/TCP 连接前返回 64。复核后 `target=0 KiB`，无 `.validation-build-*` 或 reservation。
- Release admission lightcheck 2026-07-18：默认 `run-cargo-scoped.sh` 以 12 GiB reservation + 20 GiB floor 运行 `true`，因 `available_kib=28588348 < 33554432` 返回 75；wrapper 清理自身 16 KiB 临时 target，复核 `target=0 KiB`。因此当前 release C0 阻断来自整盘可用空间不足，不是本轮 target 残留。
- Scheduler focused rerun 2026-07-18：development reservation 6 GiB 下通过 6 个精确 Rust filter：scheduler fallback toggles、preflight scheduler fallback toggles、non-terminal runtime backlog 不假禁用、finite queue lease policy、queued deadline freeze、40x15/global-500；均为 `running 1 / passed 1`，相关测试内部各含 5 轮关键循环。scope `scheduler-focused-rerun-20260718-dev6g` 清理 `size_kib=1699916 removed=true reservation_released=true`。随后 rust-analyzer/flycheck 在根 `target/` 重建约 709 MiB 可再生产物，确认 `lsof +D target` 无活跃引用后已按用户要求删除，复核 `target=0 KiB`。
- External dispatch storage 2026-07-18：`external-dispatch-storage-real-20260718-r3` 使用当前仓库隔离 PostgreSQL/Redis，3 outer rounds × 17 filters，51/51 passed；scope `1691008 KiB removed=true reservation_released=true`。两个中间红项均为测试合同误把 coordinator cold bootstrap 5 RTT 当作 steady-state 1 RTT，修正后 warm selection/acquire 仍保持 1/2 RTT。2026-07-19 storage suite 复跑同一 17 filters × 3 outer，与 runtime quarantine 合批通过；scope `1690164 KiB removed=true reservation_released=true`。
- Thinking/bare-invoke frozen CLI 2026-07-19：pre-fix frozen `70c9741b...` 复现 ordinary `sonnet` + no native reasoning schema 400；修复后 frozen `e16df13a...` 通过 bare-invoke CLI 20 cases 和 thinking wire 60 cases。report SHA 分别为 `67c9d7c9...` 与 `439e1e69...`；通用 Bash/Read `--continue` 长会话已于 2026-07-20 独立通过，真实 Kiro upstream、thinking 主动/被动长会话、MCP/search/image 仍 pending。
- Frozen loadtest 2026-07-19：L1 product binary `e16df13a...`、loadtest binary `23c04221...`，`l1_fake_upstream_12_case_matrix_r3` 为 12/12 通过；r8 product binary `131696bd...` 进一步通过 L3 9/9、L4 12/12、L5 300 秒 2281/2281 和 L5 900 秒 6821/6821，0 Redis degraded、0 capacity failure、0 429，RSS/FD gate 通过。报告 SHA 记录在 [L1 evidence](evidence/frozen-loadtest-l1-fake-upstream-20260719.md) 与 [L3-L5 evidence](evidence/frozen-loadtest-l3-l5-chaos-20260719.md)。该结果关闭当前 frozen fake-upstream L1/L3/L4/L5；Bash/Read 长 Claude CLI 也已独立通过，但两实例、真实 upstream、native MCP/search/image/agent、UI/upgrade 和 final inventory 仍 pending。
- Long-history/tool_result boundary 2026-07-19：summary SHA `927f33fe...` 覆盖 4 fake-upstream payload cases / 20 requests；on-too-long retry capture summary SHA `f85b3f9a...` 覆盖首发 too-long 后 retry trim。两者均使用 r8 product `131696bd...` 与 loadtest `23c04221...`，raw temp roots 删除，内部 transcript markers 为 0。
- Disk/Docker 2026-07-18：清理当前仓库遗留测试容器/匿名卷、dangling images 和 builder cache 后可用空间约 28 GiB → 39-41 GiB；根 `target` 多次被 rust-analyzer root `cargo check` 重建，验证中仅终止当前仓库 cargo check 并在无 live refs 时删除 root target；当前仓库 scoped target 复核为 0B。
