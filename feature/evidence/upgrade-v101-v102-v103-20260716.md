# v101/v102/v103 PostgreSQL Upgrade And Atomicity Evidence

Date: 2026-07-16

Timezone: Asia/Shanghai. Rust service logs use UTC, so some raw log timestamps show 2026-07-15.

Status: `focused-pass / final-release-binary-rebind-pending`

Scope: PostgreSQL default startup migration atomicity, deterministic startup error classification, `v0.0.101/v0.0.102/v0.0.103` backend upgrade, Redis fixture preservation, repeated startup, advisory-lock wait, failure rollback and recovery. This is not Claude CLI, inference load, browser UI, production data or final tag evidence.

## Source And Binary Identity

| Artifact | Identity |
| --- | --- |
| v101 source | `737f9f14cb831b8c4978536a850a63c8b1103195` |
| v101 binary SHA-256 | `7d35fcb8b56e8beffbce6cb3e0b0c6302cf092fdabd92881dbd89ef62ecd429e` |
| v102 source | `e9479df71ee0044cfa0da8acbf69d98c2259a66f` |
| v102 binary SHA-256 | `3b380e7da5688aff80a5e13e48c3a37b8ba9185218fcd19683ce23dd762ca7f6` |
| v103 source | `ec44f5f80fc76d49d773c0e6c82ef8d14abff28d` |
| v103 binary SHA-256 | `63be31b0d47303c25b9345ffd97b68a1552f7af0c715e4596b3c6280b122ee9b` |
| current snapshot base | `401473ca1649997bdeccf4468e3add1bdb187248` / tree `58cd3136467a70820ce69ede47a81f1084e25fb5` |
| final tracked snapshot diff SHA-256 | `289355042235405e9c07e3dab73d7e6438aead5ef79f807f4d65a2af515d71d3` |
| untracked source manifest SHA-256 | `4c7c387493390fc68f2cdd4feb2bc1bd4f9fd0115629ae175e8ad2116f77a55a` / 5 files |
| UI asset manifest SHA-256 | `8662b7b820d3b6e2300c90063f668598fb1982a88ac6069d7574e54ba8dd9c60` / 54 files |
| current release binary SHA-256 | `992214b562c468a6761a6e97e1c141359010cf891b13a84af11e89f35054929f` |

The snapshot explicitly excluded `kiro_idc_users*.txt`. Old tag binaries embed test-time UI assets, so they are used only as backend migration producers.

Current release build used Rust `1.92.0`. Both UI builds passed; the new UI emitted one existing Vite warning for a chunk over 500 kB. The final v4 Rust release build passed with dead-code warnings and took about 4m14s in an isolated target.

## Reproduction Driver

Committed driver:

```text
feature/tests/postgres-upgrade-v101-v103.sh
SHA-256 3a1bcbcdabb44e0cce39a4d63f79120126a3a8771a6685e320a6646d04ce86d4
```

Final command shape:

```bash
ROUNDS=3 \
VERSIONS='v101 v102 v103' \
RUN_ID=final3-20260716 \
RESULT_ROOT="$PWD/target/validation/f04-upgrade-20260716" \
feature/tests/postgres-upgrade-v101-v103.sh
```

The script creates and deletes version-specific PostgreSQL 18 and Redis 7 containers. It uses service ports `19131`, `19132`, `19133`; all bind only to loopback. It never sends an inference request.

## Raw Artifact Manifest

Generated directory:

```text
target/validation/f04-upgrade-20260716/
```

Key files:

| File | SHA-256 / note |
| --- | --- |
| `results.tsv` | `93f5728c6ed715581c38db2f38667f1c34a556d4979bdc7e2b094b8a1b61469b` |
| `runner.log` | `6b98c9695e75b7705784878d15519792474f22d8d601577af6bed6206cca687d` |
| `runtime/<case>/before-upgrade.snapshot` | old schema/business fingerprint |
| `runtime/<case>/after-upgrade.snapshot` | first current startup fingerprint |
| `runtime/<case>/after-repeat.snapshot` | repeated startup fingerprint |
| `runtime/<case>/*.columns.tsv` | physical ordinal plus semantic column attributes |
| `runtime/<case>/*.indexes.tsv` | complete index names/definitions |
| `runtime/<case>/*.markers.tsv` | version/checksum set |
| `runtime/<case>/failure-{1,2,3}.log` | deterministic failure/error-chain evidence |
| `classifier/*v4*` | valid v4 late-PG and bad-auth evidence |
| `classifier/invalid-random-port-*` | excluded fixture attempt |

The final matrix generated 653 files before classifier additions and occupied about 6.8 MiB. Raw artifacts remain ignored generated output; the committed driver and this summary are the reproducible evidence.

## Atomicity Red And Green

Test:

```bash
KIRO_RS_TEST_POSTGRES_URL='postgres://<isolated>' \
  cargo +1.92.0 test \
  postgres_startup_migration_checksum_failure_rolls_back_entire_default_chain \
  -- --nocapture
```

Failure fixture removes two tables and three revision/generation columns, deletes later markers, corrupts the storage-revision checksum, writes runtime/credential sentinels, then invokes default migration five times.

Pre-fix result:

```text
before: 21 tables / 267 columns / 46 indexes
after failed attempt 1: 24 tables / 307 columns / 55 indexes
failed startup migration attempt 1 left a partial schema, marker, or business-data mutation
```

Post-fix initial result: 1 passed. Final validation repeated the exact test three outer rounds; all three passed, and each test internally asserted five unchanged failure attempts.

The final v4 migration filter result was:

```text
14 passed; 0 failed; 1308 filtered out; finished in 4.65s
```

It includes the PostgreSQL/config migration tests plus the new PostgreSQL startup retry classifier and inline marker idempotency test whose names also contain `migration`. Earlier, before adding those two tests, the same filter passed 12/12 in three independent outer rounds.

## Retry Amplification Red And Green

Before the `main` classifier fix, a real v101 schema with a corrupt `credential-storage-revision-v1` marker produced:

| Attempt | elapsed | DDL notices | mismatch occurrences | final root cause visible |
| --- | ---: | ---: | ---: | --- |
| 1 | 63.901s | 1980 | 17 | no |
| 2 | 64.112s | 1980 | 17 | no |
| 3 | 63.505s | 1980 | 17 | no |

The terminal message was `PgSQL 在 60 秒内未就绪`.

After the classifier and `{:#}` error-chain logging, the formal three-version matrix contains 27 failed starts:

```text
elapsed min/max: 212.659 / 293.120 ms
mismatch occurrences per process: 1
DDL notices per process: 110..118
final error chain contains credential-storage-revision-v1 checksum mismatch: 27/27
snapshot unchanged: 27/27
```

Focused tests:

```text
lifecycle_tests: 4 passed / 0 failed
non-retryable operation calls: exactly 1
retryable operation: succeeds on attempt 3
IO connection-refused: classified retryable
checksum invariant: classified non-retryable
```

## Upgrade Matrix

Each version has 3 outer rounds. Each round has 9 recorded phases:

```text
normal:  upgrade + repeat
large:   upgrade + repeat
failure: failure-1 + failure-2 + failure-3 + recovery + recovery-repeat
```

Aggregate:

```text
3 versions x 3 rounds x 9 phases = 81
pass = 81
non-pass = 0
wall clock = 118.5 s
```

Fixture sizes:

| Scenario | usage | rollup totals | time buckets | Redis keys |
| --- | ---: | ---: | ---: | ---: |
| normal/failure | 25 | 2 | 1 | 6 |
| large | 50,000 | 5,000 | 1,000 | 40,000 |

Latency:

| Version | ordinary normal upgrade, rounds 2/3 | large upgrade, 3 rounds | checksum failure, 9 starts | lock-held startup |
| --- | ---: | ---: | ---: | ---: |
| v101 | 389.741-511.243ms | 382.987-617.596ms | 213.365-293.120ms | 1111.173ms |
| v102 | 495.722-508.419ms | 381.311-509.922ms | 212.659-291.661ms | 1129.159ms |
| v103 | 375.241-501.520ms | 381.156-506.461ms | 224.788-285.567ms | 1105.828ms |

The three lock cases first confirmed advisory lock `4950531234001` was held, kept it for 750ms, then required startup to wait at least 700ms and become ready.

## State Preservation

All current successful states have:

```text
tables=24
columns=307
indexes=55
markers=6
semantic_schema_hash=376ca14a5d995957552574ac30fa59b9
semantic_marker_hash=7f86721d704fb1e1ee2f6d7f84b29163
```

Values preserved by machine assertions:

- runtime config sentinel and version 17;
- one credential and one runtime-state row;
- usage, rollup total and time-bucket counts and content hashes;
- one external pool;
- all fixture Redis keys in the unique prefix;
- marker checksum set;
- semantic column/index set.

Revision outcomes:

```text
v101 credential/runtime/generation = 1/1/0
v102 credential/runtime/generation = 2/0/0
v103 credential/runtime/generation = 2/0/0
```

These values match tag lifecycle semantics. Repeated startup preserved them exactly.

The v3 matrix first proved every successful repeat changed `inline-schema.applied_at`; that observation was classified as an idempotency/observability defect, not accepted silently. The v4 SQL updates the rolling marker only when its checksum changes. A focused real-PostgreSQL test passed three outer rounds and covers unchanged repeat, stale checksum repair with a new timestamp, and another unchanged repeat. The v4 driver now fails immediately on timestamp churn; all 81 result rows report `metadata_churn=0`.

## Schema Convergence Audit

The first fingerprint ordered columns by `ordinal_position`, producing a v101/v102 difference. Targeted manifest runs then compared v101 and v102 after-upgrade states:

```text
full column manifest changed lines: 8
column differences after removing ordinal_position: 0
index differences: 0
marker differences: 0
```

Only four ordinal values differ:

```text
credential_runtime_state.generation  8 vs 6
credential_runtime_state.updated_at  6 vs 8
credentials.deleted_at               10 vs 11
credentials.revision                 11 vs 10
```

The final driver hashes columns by table/column name and exports ordinals separately. The final formal run therefore shows one shared semantic schema hash for all three versions.

## Real Classifier Checks

Valid fixed-port delayed PostgreSQL:

```text
delay before docker start: 1 s
service readiness: 2032.676 ms
readyz postgres/redis/redisRuntimeEvents: true/true/true
```

SQLx's initial connect remained pending until PostgreSQL became available, so this case did not invoke the outer retry warning. It proves short dependency delay still recovers; the outer retry branch is covered by the focused async test.

Valid fixed-port bad authentication:

```text
exit code: 1
elapsed: 34.030 ms
log contains: 初始化失败且不可重试
log contains: password authentication failed
```

Excluded attempt: an earlier container used Docker's random host-port mapping. Its stop/start changed the mapped port, while the service retained the old URL and correctly timed out. Files are renamed `invalid-random-port-*`; this attempt is not a product failure and is not counted in pass statistics.

## Final Static And Focused Gates

```text
cargo +1.92.0 check --tests                         PASS
cargo +1.92.0 test lifecycle_tests                 4/4 PASS
atomicity exact test, outer rounds                  3/3 PASS
cargo +1.92.0 test migration                       14/14 PASS
bash -n feature/tests/postgres-upgrade-v101-v103.sh PASS
git diff --check on main/postgres/driver            PASS
```

The compile emitted three existing dead-code warning groups; there were no errors.

## Cleanup Audit

After the formal driver:

```text
containers matching final3-20260716: 0
listeners on 19131/19132/19133: 0
```

The fixed-port classifier containers were removed by exact name. The dedicated old test PostgreSQL/Redis pair was retained only through final Rust integration tests and is removed during task cleanup. No unrelated container, schema, Redis namespace, process or worktree was deleted.

## Release Binding And Limits

- This evidence proves the candidate binary hash above, not an arbitrary later dirty tree or tag.
- If final release SHA differs, rerun the committed driver or a documented equivalent release-binding matrix.
- No production database was read or modified.
- No real credential or upstream inference was used.
- 50,000 usage rows are a bounded regression scale, not a production maximum.
- Explicit rollup compression remains a separate maintenance transaction and is not exercised by the default-startup matrix.
- Old tag UI artifacts are not authenticated historical release assets.

Issue narratives: [startup migration atomicity](../issues/postgres-startup-migration-atomicity.md) and [three-version upgrade smoke](../issues/upgrade-v101-v102-v103-smoke.md).
