# 证据索引

Role: 将问题结论关联到可重复命令、报告、capture、配置快照、构建哈希和 request ID

Status: 整理中

当前上一轮证据位于 `target/validation/deep-audit-20260715/`。该目录被忽略且只作为原始支持材料；最终结论必须在本目录记录测试命令、时间、源码 revision、二进制 SHA-256、隔离端口、配置摘要、轮次、成功/失败数、上游 attempts 和资源采样。

## 已确认的历史证据完整性缺口

deep-audit 的脚本、汇总和部分 report 存在，但真实 Claude CLI/direct/external 的逐行 JSONL、fake upstream `/stats` before/after 和每轮 build manifest 并未全部保存。因此文档中的 3/3、5/5、9x、30x 可作为当前候选的修复前定位证据，但不能在最终发布包中作为可独立重算的验收证据。

修复后每次运行必须保存脱敏后的：

- 命令与 case ID；
- Claude CLI stream-json JSONL；
- direct SSE/non-stream response；
- fake upstream stats 前后快照与差值；
- usage/request/error ID 摘要；
- Git revision、dirty diff 和 binary SHA-256；
- 启动/清理后的端口、PID、Redis/PG namespace 检查。

只提交小型脱敏摘要和 manifest；大体积原始产物留在忽略目录并记录 SHA-256。

不得写入 API key、credential、cookie、Authorization、数据库密码或 `kiro_idc_users*.txt` 内容。生产取证只允许只读、最小化和脱敏采集。

## 当前专项证据

- [2026-07-23 最终发布门禁复测证据](final-release-gate-20260723.md)：当前统一候选 `kiro-rs` SHA-256 `925525419cd48b460217df2568891a40287da0c44d2bf921a38b103c047775ee`，`kiro_loadtest` SHA-256 `90babda7388aa93854cbbdb81c132cc436c07f46b0ea22973531b0a7ffb3aff1`。最终 C0/r4 scoped batch 通过 `cargo +1.92.0 fmt --all -- --check`、`cargo +1.92.0 test --all-targets`（main `1750 passed / 0 failed / 6 ignored`，`kiro_loadtest 31/31`）和 `cargo +1.92.0 build --release --bins`；scoped target `2516216 KiB removed=true reservation_released=true`。同轮补跑 feature docs `47/47` 与 `115` links、`node --test feature/tests/*.test.mjs` 为 `283 tests / 261 pass / 22 explicit skips / 0 fail`、`git diff --check`、build artifact inventory。inventory 初次发现 rust-analyzer/flycheck 重新生成 root `target/` 约 `918 MiB`，只删除可再生 repo `target/` 输出且未停止既有 9022 后，最终 inventory 为 `targets=0 reservations=0 target_processes=0 blockers=0`。该证据不代表 Docker pass，不替代 2026-07-22 真实 Claude CLI 与 scheduler/Redis/external takeover 复测。
- [2026-07-22 当前工作树回归复测证据](final-regression-rerun-20260722.md)：当前仓库外冻结 `kiro-rs` SHA-256 `31b8c4749201b0f7666b63a9c268c0b75e21f6c1600b18c77bf39a7c6c249c2e`，Claude Code CLI `2.1.197`。本轮不执行 Docker、不触碰既有 `127.0.0.1:9022`，使用 caller-owned PostgreSQL databases 与 loopback Redis DB/prefix，完成 runtime quarantine storage `18/18`、scheduler Redis chaos `24/24`、business/observability Redis fault-domain product gate、protocol contamination/source marker contracts `30/30`、真实 Claude CLI thinking capture `30/30`、真实 kiro.rs thinking wire `60/60`、真实 Claude CLI long session `5×20`（110 turns / 100 tool pairs / leakMatches=0）、bare invoke `20/20`、reasoning/body focused Rust `8/8`。本轮临时报告目录、冻结候选、临时 pg client/psql wrapper 和 4 个 caller-owned 测试数据库已在记录摘要后删除。
- [Final candidate C0d static/CLI/load/UI evidence](final-candidate-c0d-static-cli-load-ui-20260721.md)：当前仓库外候选 `kiro-rs` SHA-256 `fefd6204c1851c9795ae16fb006115997f7884570988622a77200c3e438cd7ec`、`kiro_loadtest` SHA-256 `f92e91b4f9c2d669e29e6bbb9e4d4b58f38d2f8bfac3f4bd51260c0d2edd6782`。C0d scoped batch 通过 `cargo fmt --check`、`cargo test --all-targets`（main `1742 passed / 0 failed / 6 ignored`，`kiro_loadtest 31/31`）和 `cargo build --release --bins`，scoped target `2446284 KiB removed=true reservation_released=true`。同轮复跑静态合同：feature docs 47/47 与 108 links、UI/cost/MCP/request-key/prompt 合同通过、`node --test feature/tests/*.test.mjs` 为 `280 tests / 258 pass / 22 explicit skips / 0 fail`；Claude Code CLI 2.1.197 raw thinking capture 6 档各 5 轮通过，确认 `thinking.type=adaptive` 与 `output_config.effort=max` 原样发送；两套 UI 本地 build 通过；C0d `kiro_loadtest` fake-upstream 小矩阵覆盖 stream/non-stream/thinking/tool/429/500/JSON-exception200/malformed/client-drop/mixed/recovery。只删除无引用 root `target/debug`/`flycheck0`/`.rustc_info.json`，未停止 PID 84264 的 9022 服务；最终 inventory 为 `targets=0 reservations=0 target_processes=0 blockers=0`。该证据不替代 caller-owned PG/Redis 动态服务、真实 Kiro upstream、native MCP/search/image/agent、UI browser、upgrade、生产 recurrence 或最终 release gate，发布仍为 `NO-GO`。
- [Protocol contamination source contract](protocol-contamination-source-contract-20260721.md)：新增纯 Node 源码合同，不启动 Docker、不启动 `kiro.rs`、不调用 Cargo。单独运行 `10 tests / 10 pass / 0 fail`；与业务/观测 Redis fault-domain 合批为 `56 tests / 47 pass / 9 explicit live-signal skips / 0 fail`。合同锁定 sanitizer 不信任任意 `Hashxxxxxxxx`，只使用当前请求已知工具名、确定性映射名和 legacy overlong 映射名；raw marker-free body 在 DOM parse 前返回 `Ok(None)`；assistant 清理只改 assistant text/thinking，不改 user/tool result/tool input/unmodeled fields；signed/redacted thinking 原子 fail closed；request strict profile、stream/non-stream、external 污染后不允许空白/部分 success terminal。该证据只关闭源码防回归合同，不替代真实 Kiro upstream、active/passive thinking、MCP/search/image/agent、故障恢复、UI、upgrade 或 final release gates。
- [Protocol marker inventory source contract](protocol-marker-inventory-source-contract-20260721.md)：新增纯 Node 生产源码 marker inventory 合同，不启动 Docker、不启动 `kiro.rs`、不调用 Cargo。单独运行 `4 tests / 4 pass / 0 fail`。合同在排除 Rust test modules 与 `*/tests.rs` fixtures 后锁定：`user Continue` / `user Tool results provided` 只允许出现在 sanitizer；`Tool results:` 只允许出现在 sanitizer 与 stream bounded observability；`<function_results>`、`<function_calls>` 相关标记只允许出现在 stream 协议适配；生产代码不存在 bare `<invoke` / `</invoke>` 字面量，也不存在 `[previous output]`、`[trimmed output]`、`[duplicate output]`；tool-result-only user placeholder 为 inert dot `"."`，无效/重复/orphan tool_result 修复不把 rejected content textify 成普通正文。该证据补齐“不是只看 Hash 指纹”的生产源码面，仍不替代真实 native CLI/upstream/fault/load gates。
- [SchedulerRedisDegraded 外部池接管验证程序](external-takeover-scheduler-degraded-20260721.md)：源码路径复核确认 `fallbackOnSchedulerRedisDegraded`、fresh local state guard 和 external eligibility 的判断链；focused Rust `external-takeover-focused-20260721-r2` 四个 exact handler/fallback tests 通过，scoped target `1708372 KiB removed=true reservation_released=true`；新增非 Docker runner `external-takeover-scheduler-degraded-nondocker.mjs`，合同测试 8/8 通过，覆盖 no Docker/Cargo、no protected `9022` probe、loopback-only PG/Redis、Redis DB1..15、caller-owned prefix 和 validate-only。动态 service run 尚未执行，因为本轮没有调用者确认的独占空 PostgreSQL database URL；该证据不能替代 external takeover 产品门禁。
- [E01/E02 scheduler fairness 非 Docker runner 合同](scheduler-fairness-nondocker-runner-contract-20260721.md)：`scheduler-fairness-sticky-race.mjs` 已从旧 Docker-managed PostgreSQL/Redis 入口改为 caller-owned PG/Redis 入口；不再 Docker run/exec、不再 `FLUSHDB`、不再 `CREATE DATABASE`，每 case 使用独立 Redis `keyPrefix` 并只清理 owned prefix。合同测试 7/7 通过，runtime path 合同 9/9 通过，并复跑 external takeover contract 8/8 与 `git diff --check`。该证据只关闭 runner 安全合同；E01/E02 动态分布公平仍需冻结 binary + 预创建空 PG databases + loopback Redis 执行。
- [Strict local-first E05 非 Docker runner 合同](strict-local-first-nondocker-runner-contract-20260721.md)：`strict-local-first-routing.mjs` 已从旧 Docker/Toxiproxy-backed 入口改为 caller-owned PostgreSQL/Redis 入口；保留 10 类 E05 路由模式，要求仓库外冻结 binary、owned artifact root、`modes × rounds` 个预创建 `kiro_e05_*` database、loopback Redis DB1..15 和 caller-owned Redis prefix。脚本不启动 Docker、不创建 database、不 `FLUSHDB`、不调用 Cargo、不探测 `9022`，Redis fault 改用 `redis-chaos-proxy.mjs`，结束只清理 owned prefix；子进程使用最小环境，不继承 caller PG/Redis URL。合同测试 6/6 通过；同批 runtime path 9/9、external takeover contract 8/8、E01/E02 contract 7/7，合计 30/30 通过，`git diff --check` 通过。最小环境断言曾红于 `startService` 继承 `process.env`，修复后 E05 contract 6/6。后续低产物补证清理无引用 root target 后 inventory 为 `targets=0 reservations=0 target_processes=0 blockers=0`。该证据只关闭 runner 安全合同；E05 动态 service run 仍需冻结 binary 与预创建空 PG databases。
- [AWS API Key + region 非 Docker runner 合同](aws-api-key-region-nondocker-runner-contract-20260721.md)：`aws-api-key-region-lifecycle.mjs` 已从旧 Docker-managed PostgreSQL/Redis 入口改为 caller-owned PG/Redis 入口；不启动 Docker、不创建 database、不 `FLUSHDB`、不调用 Cargo、不探测 `9022`，Redis 清理只删除 `${KIRO_F06_REDIS_PREFIX}:*`，服务子进程使用最小环境。F06 contract 6/6 通过；同批 runtime path / external takeover / E01/E02 / E05 runner contract 合计 36/36 通过，`git diff --check` 通过。该证据只关闭 runner 安全合同；完整 F06 动态仍需冻结 binary、预创建空 `kiro_f06_*` PostgreSQL database、loopback Redis DB1..15 和 caller-owned prefix。
- [Request API Key Admission 非 Docker runner 合同](request-api-key-admission-nondocker-runner-contract-20260721.md)：`request-api-key-admission-multi-instance.mjs` 已从旧 Docker PostgreSQL/Redis/Toxiproxy 入口改为 caller-owned PostgreSQL/Redis + 本地 `redis-chaos-proxy.mjs`；不启动 Docker、不创建 database、不 `FLUSHDB`、不调用 Cargo、不使用 `host.docker.internal`、不探测 `9022`，两实例服务子进程使用最小环境，Redis 只清理 owned prefix。request-admission contract 5/5 通过；同批 request-admission / F06 / runtime path / external takeover / E01/E02 / E05 runner contracts 合计 41/41 通过，`git diff --check` 通过。该证据只关闭 runner 安全合同；完整多实例 admission dynamic 仍需冻结 binary、预创建空 `kiro_request_admission_*` PostgreSQL databases、loopback Redis DB1..15 和 caller-owned prefix。
- [Frozen Load/Chaos 非 Docker runner 合同](frozen-load-chaos-nondocker-runner-contract-20260721.md)：`frozen-load-chaos-runner.mjs` 已从旧 Docker-managed PostgreSQL/Redis 入口改为 caller-owned PG/Redis 入口；要求仓库外冻结 `kiro-rs` 与 `kiro_loadtest`、owned artifact root、tier 对应数量的预创建 `kiro_load_chaos_*` database、loopback Redis DB1..15 和 caller-owned prefix。脚本不启动 Docker、不创建/drop database、不 `FLUSHDB`、不调用 Cargo、不探测 `9022`，fake/proxy/loadtest 子进程使用最小环境，Redis 只 SCAN/DEL owned prefix。contract 6/6 通过；同批 runtime path 合同 15/15 通过，`git diff --check` 通过。该证据只关闭 runner 安全合同；最终 frozen L3/L4/L5 dynamic 仍需用改造后的 runner 重跑。
- [Redis storage runner no-FLUSH 合同](redis-storage-runner-no-flush-contract-20260721.md)：`run-token-refresh-cluster-validation.mjs` 与 `run-multi-instance-redis-coordination-validation.mjs` 已去掉 runner 级 `FLUSHDB` 兜底；启动前仍要求 caller-confirmed empty DB1..15，测试后若有残留只报告 `residualKeyCount` 并让门禁失败，不清空整库。纯 Node 合同合批为 `19 pass / 1 skip / 0 fail`，skip 仅为显式 live nonempty Redis opt-in；source 扫描确认这两个 runner 和合同文件中无 `FLUSHDB/FLUSHALL`。该证据只关闭 runner 清理安全合同；token-refresh cluster 与 multi-instance Redis dynamic 仍需在 caller-owned Redis/PG 上通过。
- [Runner 子进程环境隔离合同](runner-child-environment-isolation-20260721.md)：新增 `validation-child-env.mjs`，把真实 runtime/Claude/load/scheduler 验证 runner 的服务、Claude、proxy 和 scoped-Cargo 子进程改为白名单环境，不再继承整份 `process.env`。独立 `runtime-validation-paths.test.mjs` 当前 11/11 通过，覆盖仓库外 frozen binary/artifact、禁止 direct Cargo output、禁止 9022 listener probe、白名单环境不泄漏 `DATABASE_URL`/`REDIS_URL`/Anthropic/OpenAI/API key/任意 `KIRO_*`，并静态确认所有非测试 runner 无 `...process.env`。该证据不启动 Docker/Cargo/kiro.rs，只关闭验证污染合同；动态服务门禁仍 open。
- [轻量合同回归](lightweight-contract-regression-20260721.md)：本轮主体不启动 Docker、不启动 kiro.rs 服务，追加复核文档/UI/prompt/runner 安全/信号/wrapper lifecycle。早期 feature docs 47/47 与 102 links 通过；runner 子进程环境补证后复跑为 47/47 与 104 links。cost format、MCP attempt channel、request API key ID、prompt control independence/default parity 均通过；E03/token-refresh/multi-instance runner contracts 70 pass/1 skip，scheduler chaos/fault-domain contracts 初始 44 pass/21 skip、业务/观测 Redis 源码合同补证后合批 49 pass/21 skip/0 fail、RedisStore production role guard 补证后合批 50 pass/21 skip/0 fail、主/观测 Redis 路径隔离合同补证后合批 53 pass/21 skip/0 fail，thinking wire 45/45，Claude capture/bare signal 5/5，scoped wrapper lifecycle 21/21。最新继续复核：UI/prompt 合同 PASS，protocol contamination source contract 10/10，protocol+Redis fault-domain 合批 56 tests：47 pass、9 explicit live-signal skips、0 fail，non-Docker runner/path 合批 49/49，E03/token-refresh/multi-instance/scheduler/fault-domain 合批 146 tests：124 pass、22 explicit fixture skips、0 fail，thinking/Claude signal 合批 50/50。此前 non-Docker runner/path 合同合批为 68 pass/1 skip/0 fail，skip 为显式 live nonempty Redis opt-in，不计产品 pass。RedisStore role guard 另用 scoped `cargo +1.92.0 check --bin kiro-rs` 通过，scope `446876 KiB removed=true reservation_released=true`。清理无引用的可再生 `target/debug`、`target/flycheck0` 和 `.rustc_info.json` 后，inventory 为 `targets=0 reservations=0 target_processes=0 blockers=0`；本轮又只清理 5 个明确测试前缀小临时目录。文档补证后 feature docs 47/47 与 106 links 通过，首次 inventory 因 root target 约 710 MiB 与 PID 84264 引用 fail；只删除无引用 debug/flycheck/.rustc_info 后 `target=0B`，inventory 重新为 `targets=0 reservations=0 target_processes=0 blockers=0`，未停止 PID 84264；Docker 只读盘点超时仍是 manual-only hint。
- [业务/观测 Redis 故障域产品验证](business-observability-redis-fault-domain-20260721.md)：使用当前项目两个 loopback Redis authority（早期业务 `26379/15`、观测 `50892/15`；后续回归业务 `26379/8`、观测 `50892/2`）完成纯 Node 早拒绝/信号合同、基础 Redis fault-domain runner 3 outer、产品 runner `redis-fault-domain-product-r2` 与修复后回归 `redis-fault-domain-product-20260721-r4`，以及 Rust 1.92.0 scoped C0 子集。后续源码合同、RedisStore role guard 与主/观测 Redis 路径隔离合同补证把默认纯 Node contract 扩展到 46 tests：37 pass、9 live signal skips、0 fail，锁定 `main.rs`、`UsageRecorder`、`AdminService`、`RedisStoreRole`、Redis usage materialization entrypoint guard、config env/authority guard、scheduler/external/runtime-event/health 的 business Redis 专用路径、observability Redis 启动失败不回落 business Redis、以及 UsageRecorder 主请求路径只入队观测 writer 且压力下丢弃 summary 以避免阻塞；scheduler chaos + fault-domain 合批为 74 tests：53 pass、21 live-fixture skips、0 fail，底层 guard scoped `cargo +1.92.0 check --bin kiro-rs` 通过。产品 runner 验证 observability latency/disconnect 不影响 business scheduler，business Redis fault fail closed 且不伪装 `AllDisabled`，observability 不回落 business Redis；所有 scoped targets `removed=true / reservation_released=true`。该证据只关闭 E09 focused/product/source-contract gate，不替代两实例真实服务、external takeover dynamic、真实 upstream/CLI、多能力 native gate、UI、upgrade 或 final release inventory。
- [Claude Code CLI `--continue` 长会话回归](claude-cli-long-session-continue-20260720.md)：使用 frozen product `131696bd...`、真实 Claude Code CLI 2.1.197、隔离 kiro.rs、fake Kiro EventStream 和 caller-owned PG/Redis，完成 1x2 smoke、5x20（110 turns/210 inference/100 tool pairs）与 5x100（510 turns/1010 inference/500 tool pairs）。Bash/Read 映射名往返、session ID/resume、history/tool_result pairing、非零 usage、body/history 增长和 hash/scaffold marker 扫描全部通过；两轮报告 SHA 为 `f8a5faa3...`、`cd79f11b...`。该证据只关闭 frozen fake-upstream 的真实 CLI 长会话门，不替代真实 Kiro upstream、native WebSearch/MCP/image/agent、故障恢复、两实例、UI、upgrade 和最终 inventory。
- [Scheduler Redis 非 Docker chaos](scheduler-redis-chaos-nondocker-20260720.md)：使用当前项目 Redis 的空 DB 和 loopback TCP proxy，在不启动 Docker/不触碰 `9022` 的情况下完成 scheduler/lease/断连/usage-writer 联合故障矩阵。2026-07-20 `r4` 为 8 exact × 3 outer（24/24）；2026-07-21 `r5` 真实红于 WRONGTYPE recovery，把 deterministic Redis response error 误判为 commit-unknown，导致无必要 release/tombstone 写入并竞争 breaker 恢复；修复分类后 `scheduler-redis-joint-chaos-20260721-r6` 再次 24/24 通过。覆盖 50/500ms latency、指数 breaker、disconnect/reconnect、300 lease 异步释放、取消、commit-unknown、usage-writer 同窗 fault 和 WRONGTYPE recovery；0 `AllDisabled` 误分类，数据库/代理/端口/temp/scoped target 全清理。另有 runner 负向/信号合同：默认 16/16 纯 Node，显式 DB15/DB14 后完整 28/28。该证据关闭单实例 Redis chaos 与单实例 simultaneous usage-writer fault 子项，不替代两实例、external 接管和最终 release gate。

- [2026-07-19 长历史 tool_result 边界与 on-too-long retry 捕获](long-history-tool-result-boundary-20260719.md)：复核 r8 frozen candidate 在 fake upstream capture 中的长历史/tool_result 红项。preemptive large-tool-results、mixed-pathological、schema-key-mapping 与 on-too-long large-tool-results 共 20/20 请求通过；所有 captured Kiro text fields 中 `user Continue`、`Tool results provided`、`function_results`、`[previous output]` 等内部 transcript 指纹均为 0，tool_use/tool_result 无 orphan。`old_history_entry_with_large_tool_result` 与 `summarized_history_entry_with_hash_and_excerpt` 只作为合法 tool_result 夹具文本保留/截断，不能作为泄漏 oracle。另用 fake Kiro too-long 首次 400 捕获证明 on-too-long 路径 exactly 2 inference sends，首发约 554 KiB、retry 约 37 KiB，retry 后无内部 transcript 指纹。
- [2026-07-19 冻结 loadtest L3-L5 burst/chaos/soak](frozen-loadtest-l3-l5-chaos-20260719.md)：记录历史 frozen product `e16df13a...` 与 frozen loadtest `23c04221...` 在当前项目隔离 PostgreSQL/Redis 和假 Kiro upstream 下的 L3/L4/L5 结果。历史 L3 burst/recovery 9/9、L4 restart/failure/client-drop/mixed-chaos 12/12 通过；历史 L5 900 秒 long-stream soak 红灯，`5949` success / `94051` 429，300 秒诊断 `2203` success / `12361` 429，raw log 证明 `占用 Redis 凭据并发槽超过共享总期限 75ms` 打开 capacity breaker。r6/r7 先后降低 quick 429 与内部 spin，但 900 秒仍复现 degraded/429。r8 candidate `131696bd...` 将 capacity/queue budget 调整为 250ms、affinity 保持独立 75ms、capacity timeout 需连续触发才打开 breaker；r8 L5 300 秒 2281/2281、r8 L5 900 秒 6821/6821，均 0 个 429、0 capacity breaker failure、RSS/FD gate 通过；r8 L3 burst/recovery 9/9 与 r8 L4 restart/failure/client-drop/mixed-chaos 12/12 通过。2026-07-20/21 又分别关闭 fake-upstream Bash/Read `--continue` 长会话、单实例普通 Redis chaos 和单实例 usage-writer+Redis fault；真实上游/native tool/search/image/MCP、两实例 fault/fallback、UI/browser、upgrade 和 final inventory 仍 pending，发布保持 `NO-GO`。
- [2026-07-19 冻结 loadtest L1 fake-upstream 矩阵](frozen-loadtest-l1-fake-upstream-20260719.md)：记录当前仓库外 frozen product binary `e16df13a...` 与 patched frozen `kiro_loadtest` `23c04221...` 在假 Kiro EventStream/模型发现上游下完成 `l1_fake_upstream_12_case_matrix_r3`，12/12 通过，覆盖 normal stream/non-stream、thinking stream、tool_use stream、slow first text、slow thinking、JSON exception 200、429、500、invalid tool format、malformed SSE 和 client drop。该证据同时记录了前置 loadtest 夹具缺陷（未模拟 ListAvailableModels、未识别 CLI EventStream target、thinking payload budget 无效、未识别 native Kiro `output_config.effort` 等）及 scoped Cargo 清理证明。该文件本身只关闭 L1 fake-upstream smoke；另有 r8 L3/L4/L5 与 Bash/Read 长会话独立 pass。真实上游、两实例、UI、upgrade 和最终 release inventory 仍 pending。
- [2026-07-19 冻结 Claude CLI thinking/bare-invoke gate](frozen-claude-cli-thinking-and-bare-invoke-20260719.md)：记录 pre-fix frozen binary `70c9741b...` 在真实 Claude Code CLI 2.1.197 普通 `sonnet` 请求下复现 `400 model claude-sonnet-4 does not advertise a native reasoning effort field`；修复后 frozen binary `e16df13a...` 通过 bare-invoke real CLI 20 cases（15 negative literal XML、5 structured Bash tool loop，25 inference，tool_use/tool_result 各 5，cleanup 全 true）和 thinking wire 60 cases（CLI/IDE × 6 effort × 5，inference 60，model discovery 2，unknown/invalid/violations 0，cleanup 全 true）。该证据关闭 fake-upstream frozen runtime 的两个协议门；通用 Bash/Read `--continue` 长会话已在 2026-07-20 独立通过，但 thinking 主动/被动长会话、真实 Kiro upstream、MCP/search/image、UI、upgrade 和 final inventory 仍 pending。
- [2026-07-19 隔离 Redis/PostgreSQL storage validation 与构建残留门禁](storage-integration-and-artifact-gate-20260719.md)：记录当前仓库专属隔离 PostgreSQL/Redis 下 token refresh Redis 3 outer × 5 exact、Redis usage writer 3 outer × 2 exact、external dispatch 3 outer × 17 exact、runtime quarantine 3 outer × 6 exact 均通过；三个 scoped target 分别约 1.69 GiB 且 `removed=true / reservation_released=true`。随后关闭 19 天前 `kiro_cli_repro` Claude CLI/MCP tmux 验证残留，只删除无进程引用的 `target/debug`/`flycheck0`，复核根 `target=0B` 且 `inventory-build-artifacts --gate` 为 `targets=0 reservations=0 target_processes=0 blockers=0`。后续 frozen L1/L3-L5、Bash/Read 长会话、正常 usage/scheduler 联合 burst 和单实例 Redis chaos 已分别通过；真实上游、两实例、UI、upgrade 和最终 release inventory 仍 pending。
- [外部池权威选池与 dispatch fence](external-pool-authoritative-dispatch-20260718.md)：记录每请求完整 PG list、32 hard cap、prepare 前 fence 和坏行宽松默认的源码复现；manager-owned generation-bound 250ms singleflight、100ms failure cache、list/fence c128 fault suppression/recovery、in-flight-only fence、prepare 后 revision linearization、strict row isolation 与聚合日志已实现。Rust 1.92.0 all-target check 三次无 warning，11 个无存储 body/parser exact filters 实际通过；2026-07-18 使用当前仓库隔离 PostgreSQL/Redis 完成 3 outer rounds × 17 filters，即 51/51 passed。两处旧红项是测试合同误把 coordinator cold bootstrap 5 RTT 计为 steady-state 1 RTT；修正后 warm selection/acquire 仍保持 1/2 RTT。冻结负载、两实例和 release gate pending。
- [完整默认单测树红绿链](full-unit-tree-red-green-20260718.md)：记录两次 debug stack abort、四个普通断言失败、invalid-refresh 无意义 client 构建、provider fault matrix 资源竞争、24-warning 清理、迟到 HTTP response 红项、`r11` 绿树，以及 queue/storage/provider fixture 后当前 `r12` 的 `1715 passed / 0 failed / 6 ignored`；当前 scope `size_kib=1682460` 且 `removed=true / reservation_released=true`。这是 dirty-tree default-bin unit pass 和 all-target check pass，不替代真实 storage、all-target tests/no-default/release/frozen CLI/load。
- [运行时饥饿下 HTTP deadline](http-deadline-runtime-starvation-20260718.md)：记录 1 秒 header deadline 在完整树压力下于 1.651 秒接受 500 的真实红项、deadline-first 修复、10 轮顺序单测、真实 header/body stall、完整 transport/body fault matrix 和最终 1714-test 绿树；冻结 CPU/IO/load 仍 pending。
- [上游错误诊断隐私与正文边界](upstream-error-privacy-bounds-20260718.md)：记录 13 类 status/JSON 与 6 类 transport/body 故障在 stream/non-stream、1/20/60 pool、5 轮下共 570 个 provider outcome/1530 个预算 send；error、attempt、scheduler snapshot、DEBUG log 的 private marker 均为 0，Router usage 与 auxiliary 路径也在完整树覆盖。持久 PG/Redis 与冻结 HTTP/load 仍 pending。
- [本地容量预检竞态与 external fallback 延迟](local-capacity-preflight-race-20260718.md)：记录 static eligibility 已确认但 runtime hint absent 时进入本地默认 120 秒队列的候选缺陷、删除 hint authority 后的五轮 policy pass、all-targets check 与 scoped target 回收；三个 storage filter 因缺隔离 PG/Redis 提前返回，只计编译不计动态 PASS。
- [高并发低 RPM 与 runtime quarantine](high-concurrency-low-rpm-runtime-quarantine-20260717.md)：记录 PgSQL mutation backlog 与 dispatch quarantine 拆分、40 账号非终态 backlog、40x15/60 RPM/global-500、bounded queue/release/global-unlimited 对照，以及两次 `running 0 tests` 空 filter 的无效证据边界。2026-07-20 正常 usage/scheduler 联合 burst、40 账号 backlog、40x15 和 finite-waiter 组合在 3 outer 中全部通过，随后单实例 Redis latency/disconnect chaos 21/21 通过；2026-07-21 单实例 usage-writer+Redis fault r6 24/24 通过，external takeover focused/runner contract 也通过。external 接管动态服务、两实例 fault/fallback、真实上游和最终 release 仍 open。
- [有限 queue lease 与内部 Redis RPM 放大](queue-lease-rpm-amplification-20260718.md)：确认默认 finite waiter 的初始 TTL 已覆盖完整等待期但旧 guard 仍每 20 秒续租；修复 local/external TTL、长 override、unlimited renewal 与 request deadline 冻结。当前 500 guard、local/external policy、40x15/global-500、runtime config 变更和真实 API/MCP final-attempt 聚焦多轮通过；2026-07-19 真实隔离 PostgreSQL/Redis 的 pool pressure、Redis 22 秒 deadline、degraded waiter 和 cancel release 三轮动态通过，2026-07-20 正常 usage/scheduler 联合 burst 与单实例 Redis chaos 也通过；2026-07-21 单实例 usage-writer+Redis fault r6 24/24 通过，external takeover focused/runner contract 通过。两实例和 external 接管动态服务仍 pending。
- [Redis usage writer 原子性](redis-usage-writer-atomicity-20260718.md)：记录旧 3 RTT/late gate/early seen、2 RTT 中间修复的取消窗口、当前单 Lua 1 RTT 提交和单批共享 deadline；两个无 Redis 合同测试各内部 5 轮通过。2026-07-18/19 当前仓库隔离 Redis 的 cache-read cap 与 partial-error 动态程序通过；2026-07-20 正常 usage/scheduler 联合 burst 又完成 9 个测量轮，writer p99 最大 `31.482 ms`、scheduler loaded/recovery p99 最大 `55.785/69.204 ms`，RSS 增量与 FD 门禁通过。最终联合故障从 `r4` 扩展到 2026-07-21 `r6`，后者先由 `r5` 红项确认 deterministic Redis response error 不能按 commit-unknown 处理，再完成 24/24 exact invocation；当前仍待跨实例和生产高基数 p95/p99。
- [Redis usage writer + scheduler 联合故障](scheduler-redis-chaos-nondocker-20260720.md)：最终 `scheduler-redis-joint-chaos-20260721-r6` 为 8 tests × 3 outer，即 24/24；联合 exact test 内部再跑 3 轮，低延迟 16/16 且严格一写一 RTT，500ms 精确三次 failure 后 breaker fail-fast，WRONGTYPE 不误报 `AllDisabled`，disconnect 在 writer RTT 在途后注入且无隐藏 retry，硬故障移除后 5/5 恢复；RSS 约 +10--12 MiB、FD +4、target 自动回收。`r5` 在 WRONGTYPE recovery 真实失败，定位并修复了 deterministic Redis response error 被错误当成 commit-unknown 的内部写放大问题。该证据关闭单实例同时故障，不替代两实例真实进程、external takeover 和生产高基数。
- [Redis 两实例协调](multi-instance-redis-coordination-20260720.md)：当前项目隔离 Redis 上 3 outer × 5 internal，即 15/15；四个独立连接/manager 的 lease 唯一性、交叉 release、touch、崩溃 TTL recovery、共享 queue `4/16` 与 RPM reservations 均通过，最终 queue/lease=0。2026-07-21 修复后回归 `multi-instance-redis-coordination-20260721-r3` 使用 DB7 再次 15/15，scope `1708432 KiB removed=true reservation_released=true`。这是协调层 gate，不冒充两个真实服务进程 SIGKILL/restart。
- [验证构建产物生命周期与磁盘安全](../issues/validation-build-artifact-lifecycle-and-disk-safety.md)：记录根 `target/` 约 50 GiB、数据卷仅余 446 MiB 的现场，定点清理到 383 MiB/54 GiB，以及 scoped target、禁用 incremental、preflight、预算和 success/failure/signal 自动清理门禁。2026-07-18 又把 6 个 runtime/CLI runner 纳入仓库外冻结 binary/owned artifact root 与 no-9022-listener-probe 静态合同，扩展 Node 集合 `85/85` 通过。
- [External pool Redis 协调与 release](external-pool-redis-coordination-release-20260716.md)：记录 WRONGTYPE 池级隔离、clean/disconnect/restart epoch、同/跨 manager active heartbeat fencing、10k 真实 lease commit-unknown 恢复、65,536 permit 上限、shutdown drain，以及 21-cell RTT/资源完整数字；同时保留不同 PG authority 共用同 Redis prefix 和 debug 高并发延迟为未关闭边界。
- [WebSearch/MCP 协议、usage、取消与隐私](websearch-mcp-protocol-usage-privacy-20260716.md)：记录 stale-query 与 MCP body cancel 两个动态红项、13 类错误/恢复 `70/70`、stream/non-stream/zero/non-text/long-history、usage/privacy、显式 MCP attempt channel、1/20/60 credential 硬预算和两 UI/static gate；native Claude CLI 与 profile/refresh/catalog request-scoped attribution 仍明确 pending。
- [Handler EventStream 与 runtime stack 矩阵](handler-eventstream-runtime-matrix-20260716.md)：区分 provider JSON typed retry 与 handler EventStream precommit retry，记录终止状态、postcommit text/thinking/tool、usage/privacy、WebSearch/MCP 和 debug/release 栈边界；最终冻结 release HTTP/CLI/load 仍待完成。
- [远程多模态资源与 SSRF 聚焦证据](remote-multimodal-resource-and-ssrf-20260716.md)：source/byte/attempt/deadline、transport DNS 绑定、redirect 私网拒绝、取消恢复和全局准入。
- [v101/v102/v103 PostgreSQL 升级与原子性](upgrade-v101-v102-v103-20260716.md)：真实 tag binaries、三版本三轮共 81/81 phase、50,000 usage/40,000 Redis、大锁等待、27 次 checksum failure 全回滚、确定性错误 fail-fast、stale inline checksum repair 与重复启动 marker 零写；最终 tag binary 仍需发布绑定。
- [工具 hash 与泄漏时间线](tool-hash-and-leak-timeline-20260716.md)：区分 2026-03-31 的 `_xxxxxxxx` 短名、2026-05-29 的 `Hashxxxxxxxx` 指纹和后续 transcript/prompt/history 变化，证明当前 hash 格式早于 v0.0.94/v0.0.95，不能单独解释后续频率上升。
- [Strict local-first 与 scheduler chaos](strict-local-first-and-scheduler-chaos-20260716.md)：七种 local/external 状态、真实 CapacityFull holder、Toxiproxy 150 ms Redis degraded 红绿、恢复与 startup auxiliary 4-hit 上限；记录了 degraded 状态仍带 dispatchable 时错误压制 fallback 的实质缺陷及修复，同时保留 1.5-2.0 秒异常路径延迟为未关闭性能项。
- [Usage cleanup storage integration](usage-cleanup-storage-integration-20260716.md)：隔离 PostgreSQL/Redis 的持久任务、watermark、soft/hard rollup 一致性、legacy cost、duration、高基数零行清理、Redis guarded commit、fallback 性能与 scheduler burst；同时记录 same-ID soft-tombstone 合同漂移、hard-delete 后防重边界、in-flight PostgreSQL commit transaction guard 及待重跑状态。
- [Usage Dashboard P95 与时间窗语义](usage-dashboard-p95-window-semantics-20260716.md)：静态红证据确认两存储都把 duration maximum 冒充 P95，并确认滚动窗口的 Redis/PgSQL 下边界人口不同；给出 weighted nearest-rank SQL、精确首尾小时方案、1..100/重权/跨 hour/cleanup 矩阵及索引/query-plan 门禁，Rust 修复与共享 build 动态证据待补。
- [Redis scheduler fallback v5 聚焦证据](redis-scheduler-fallback-v5-20260716.md)：配置兼容迁移、分类、有限等待和两套 UI 构建已通过；真实 PgSQL/Redis fault injection 与双实例 chaos 仍待执行。
- [Evidence skill 本地验证](evidence-skill-local-validation-20260716.md)：零依赖结构校验与假 secret 打包 3 轮确定性验证通过；记录了首次 Authorization 漏项及修复后的 archive 扫描结果。
- [Payload 图片 decoded-byte 边界](payload-image-decoded-boundary-20260716.md)：记录修复前编码长度误判、修复后 `5 MiB-1/= /+1`、data URL、Kiro/Anthropic 和单一占位聚焦结果；真实格式、CLI 和性能仍待验证。
- [Payload 历史性能与 raw identity](payload-history-performance-and-identity-20260716.md)：记录历史逐 turn 重序列化及第一次批量实现中的隐藏二次扫描、最终单批次/小常数序列化结果，以及 1 KiB 至 5 MiB clean raw byte identity；release p95、50 MiB burst 和 L5 仍待验证。
- [Provider 400 分类与换号边界](provider-400-retry-classification-20260716.md)：真实本地 HTTP provider 链路覆盖 10 类错误、1/20/60 账号和 5 轮，确定性错误严格 1 hit、可恢复类受共享 4-attempt 上限；真实 CLI 与错误恢复仍待验证。
- [ListAvailableProfiles auxiliary RPM bound](profile-arn-auxiliary-rpm-bound-20260716.md)：记录同 credential 16 并发从修复前 16 hit 收敛到每轮 1 hit，403/500 短追发 0 hit、恢复、1/20/60 账号五轮、ID 复用、状态饱和、已有 ARN 5000 次零工作和 inference/auxiliary 分账；统一候选、多实例、usage attribution 与 L3/L5 仍待验证。
- [External fallback auxiliary attribution 与 PostgreSQL usage 复核](external-fallback-auxiliary-attribution-20260717.md)：真实 refresh/profile 到 external fallback 两类各 5 轮，逐轮核对 fake HTTP hit 与 inference/auxiliary usage；补齐 weighted P95、UTC 精确边界、`u64::MAX` 饱和的 outer 2/3，并记录一次不计数的测试夹具认证失败及两个 scoped target 的独立清理证明。
- [Token refresh 失败波、自动恢复与集群 RPM focused evidence](token-refresh-failure-wave-and-cluster-rpm-20260717.md)：记录短 TTL 16 caller/30 sends、timeout 32 caller 放大、旧 401 force-refresh fan-out、历史 no-Redis capacity 误分类，以及 post-correction 60/8、limit/config/revision 与 API/MCP final-attempt zero-refresh 各五轮通过；2026-07-19/20 两次真实隔离 Redis 三轮动态均通过，后一轮还复跑 API/MCP final-attempt，scope `1698460 KiB removed=true reservation_released=true`。2026-07-22 两实例 PostgreSQL/Redis cluster gate 修复前红于 fast 500 `endpoint hits=2`，修复后 7 exact × 3 outer × 5 internal（105 内部轮次）通过，覆盖 rotating/non-rotating success、PG CAS stale fence、fast failure replay、health-claim reclaim 和 stale leader fence，scope `1715296 KiB removed=true reservation_released=true`。普通 scheduler Redis 单实例 chaos 已由独立证据关闭，但完整 invalid-bearer provider、token-refresh-specific Redis slow/error/restart、usage attribution、真实 upstream/native CLI 与 frozen candidate 仍 pending，结论保持 `NO-GO`。
- [OAuth auxiliary budget、取消与跨账号放大](oauth-auxiliary-budget-and-cancellation-20260718.md)：记录取消后 detached refresh child 暂占 permit 的 21/22 红项、结构化取消修复后的 23/23，128-account shared 与 20-account independent 的 12 类 x c1/c8/c32 x 5 轮、32-waiter singleflight、默认 60/8 以及所有 scoped target 回收；单实例隔离 Redis 状态机与 API/MCP final-attempt 已在后续证据通过，Redis/PG 两实例、真实 invalid-bearer provider/CLI、usage attribution 和最终 frozen refresh/load 仍 pending。
- [Provider transient retry target guard](provider-transient-no-retry-target-20260718.md)：记录 full `cargo test --all-targets` 中 provider transport/body fault matrix 的 header-timeout 红项、瞬态失败后无备选凭据时不再进入下一轮 scheduler acquire 的 API/MCP 修复、focused fault matrix `1/1`、all-target check pass、all-target development pass `1724/0/6 + 27/27` 和所有 scoped target 回收；当前因本机磁盘不足使用 7-10 GiB development reservation，不能替代最终 12 GiB release gate。
- [协议污染 fail-closed](protocol-contamination-fail-closed-20260716.md)：记录空白成功/静默截断缺陷、hash 与非 hash 指纹、thinking/signature/redacted 原子边界、local/external stream/non-stream、多 data/逐字节/EOF 多轮状态机结果；真实 CLI、20-tool/120k history、HTTP 故障注入与最终性能仍待验证。
- [Bare invoke 正文/工具执行边界](bare-invoke-protocol-20260717.md)：记录 bare/unknown/malformed/incomplete XML 保留正文、严格 function-calls 包络恢复、上下文/prompt-injection 反例、逐字节和随机分块、结构化去重，以及完整 stream `93/93` 回归和逐批 target 清理；真实 Claude CLI runner 已准备但尚未执行。
- [JSON whitespace 逐字节语义](json-whitespace-byte-semantics-20260716.md)：真实红测证明旧 `Value` round trip 会重排/折叠键并改写数字；lexical 修复后 token、escape、无效/关闭 identity 与 1 KiB 至 5 MiB 五轮 focused matrix 通过，provider 集成和 release 负载仍待补。
- [Prompt 控制总开关](prompt-control-independence-20260716.md)：prompt master 关闭时抑制语言/task/tool-choice/thinking/chunked 代理新增提示，结构化 tool filtering 与客户端显式字段保持；Rust focused、两 UI 源码合同和默认提示 parity 通过，浏览器、Admin API、external 和真实 CLI 仍待补。
- [Endpoint body single-pass](endpoint-body-single-pass-20260716.md)：真实红测证明 CLI no-op 也会重排/规范化 body；修复后 CLI/IDE 无变更 exact identity 和有变更 full Value diff 共 30/30，通过 single parse/serialize 合并，provider 50 MiB 负载仍待补。
- [Body / Payload identity matrix](body-payload-identity-matrix-20260717.md)：同一 disposable scoped target 中完成 payload guard、whitespace、body-limit、多模态、external raw/normalized、provider 80/80、CLI/IDE debug 与四档 release probes；记录 149 个 debug test functions、35 个 release probes、输出截断的精确性能证据边界，以及 `size_kib=2410696 removed=true reservation_released=true` 和独立零残留复核。
- [Claude CLI thinking/effort 入站捕获](thinking-effort-claude-cli-ingress-20260717.md)：真实 Claude Code CLI 2.1.197 对 absent/low/medium/high/xhigh/max 三次各 5 轮，共 90 个独立 session；确认 adaptive 恒存在、absent 默认 high、显式 max 不被 CLI clamp，并记录官方 Kiro IDE 1.0.165 动态 effort schema 的静态交叉证据、runner timer 修复。2026-07-18 又修复旧 runner 对 live 9022 listener 的隔离探针，重跑 signal `9/9` 与完整 capture `30/30`，当前报告只含 `protected9022ProbeSkipped:true`。frozen fake-upstream Kiro wire 已 60/60 通过，证明 `max` 不截断且未声明 schema 不发明 `thinking`；通用 Bash/Read 长会话已独立通过，真实上游、主动/被动 thinking 长会话、tool/search/image/MCP 仍 open。
- [Thinking/effort Kiro wire runner 安全硬化](thinking-effort-kiro-wire-runner-hardening-20260717.md)：记录 tracked-only source identity、外部冻结 binary 与 direct Cargo output 拒绝、统一有界 TERM/KILL、timer 取消、server socket ownership，以及纯 Node path/contract `11/11`、生命周期 `42/42` 和零临时残留；未启动 kiro.rs/Claude/PG/真实 Redis，不能替代最终 Kiro wire，发布仍为 `NO-GO`。
- [Thinking/effort 开发级最终 wire](thinking-effort-development-wire-20260718.md)：当前源码 `thinking 104/104`、`effort 17/17`、provider 80 次字节捕获和 converter-to-provider `max` 20 次最终请求通过；真实 Claude CLI ingress 后的 frozen fake-upstream Kiro wire 60/60 也通过，证明 `max` 不截为 `high`，Kiro schema 只声明 `output_config` 时不伪造 `thinking`，并记录 Claude CLI `HEAD /cc` 探针为协议事实。通用 Bash/Read `--continue` 长会话已独立通过；真实上游、主动/被动 thinking 长会话、tool/search/image/MCP 仍 pending，发布保持 `NO-GO`。
- [Kiro thinking 协议与开源实现交叉核验](kiro-thinking-open-source-crosscheck-20260717.md)：记录官方 Kiro bundle 的动态 schema、严格 signed/redacted history union 与首 chunk 前单次 signature retry，并对照三个 GitHub 项目的固定 commit；2026-07-18 复核 `chaogei/Kiro-account-manager` 当前公开说明和本地源码，补充其 history 丢弃 `reasoningContent` 的兼容选择，继续明确第三方 substring retry、silent effort fallback/drop、invented adaptive、redacted merge 或 history-drop 都不能作为本项目最终合同。
- [AWS API Key 与 region 生命周期](aws-api-key-region-lifecycle-20260716.md)：记录 CLI-family transport override、region Host/Bearer/tokentype、Admin API/JSON/plain 十个完整运行 90/90 正向，以及 pipe/显式 region/plain 153/153 malformed 的 0-upstream/0-PG 拒绝；覆盖 PG/reload/scheduler、重复导入零 auxiliary hit、显式备份安全头和清理。最终候选 SHA、双 UI 浏览器与多实例重复竞态仍待验证。
